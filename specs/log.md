# Mapsift decision log

> **What this is.** A grep-able derived index of foundation versions and closed decisions. It is **not a source
> of truth.** The foundation's section 15 changelog is the authority for version history, and the document named
> in each line is the authority for that decision; this file only mirrors them in flat, greppable lines so a
> version or a decision can be found without reading a full changelog. If this file and the foundation disagree,
> the foundation wins and this file is the one to fix.
>
> **Format.** One line per entry: `## [YYYY-MM-DD] <type> | <one-line description>`. The types in use are
> `version` (a foundation version bump, mirrored from section 15), `decision` (a decision closed in a derived
> document that did not bump the foundation), `finding` (something a review, a window or a probe discovered
> that generalizes past the round it was found in), and `trap` (a tool or an environment behaving in a way
> that looks right and is not, recorded so the diagnosis is paid once). **The file is the authority for that
> set and this paragraph is not**, so grep the whole file when you do not know which type a thing was filed
> under. *Corrected 2026-08-14, where this paragraph named two types over a file already using four, so a
> reader following it reached two thirds of the entries with nothing telling them the rest existed.*
>
> **A wrong entry is corrected in place with a dated note of what changed, never deleted and never appended
> over** (settled 2026-08-14). A later entry contradicting an earlier one leaves two lines that one grep
> returns together without saying which won, which is the worst outcome available in the one file whose whole
> purpose is to be grepped.

## [2026-06-23] version | v0.1 — initial foundation: server-authoritative-with-offline thesis, elements/layers frontier, the sync mechanism, offline domain limits, Lightning for large data, analysis as a served layer, MapLibre rendering, tenant isolation; opened OQ-1 to OQ-7; CRDT demoted to a gated candidate.
## [2026-06-23] version | v0.2 — adversarial review: conflict by granularity with preserve-not-discard, legal-weight classification, de-gated Lightning, fixed the sync-tier role, one client persistence layer, the MapLibre editing restriction, fixed I4 and I6; opened OQ-8 to OQ-13.
## [2026-06-23] version | v0.3 — vocabulary purge (release-version language removed), the scope-policy statement, the capability-layer decision; opened OQ-14.
## [2026-06-23] version | v0.4 — cross-platform architecture and the shared Rust core: multi-platform target, the Rust client core (WASM and FFI), Flutter mobile, the portability principle, the design-system catalog, the PyO3 conflict-rule (later superseded).
## [2026-06-23] version | v0.5 — product philosophy (section 0.5) and the design rule.
## [2026-06-23] version | v0.5.1 — patch: fixed dangling references found in a line-by-line review; no decision changed.
## [2026-06-23] version | v0.6 — replaced PyO3 with golden-tested rule equivalence (I8), reframed shared topology (OQ-1), added the non-MVP reasoning, reconciled the Celery and PostgreSQL-16 governance drift.
## [2026-06-23] version | v0.7 — state reconciliation and idempotency (I9), identity in the offline context (I10), the data-privacy posture (OQ-16), ratified TiTiler and S3/MinIO.
## [2026-06-23] version | v0.8 — authoritative authorship in three levels (I10), the client definition and ack echo (I9), offline-store protection (OQ-17), the offline authorship-proof question (OQ-18).
## [2026-06-23] version | v0.8.1 — patch: added Mermaid diagrams for readability; no decision changed.
## [2026-06-24] version | v0.9 — scope: depth versus breadth separated, general-purpose technical depth, the closed native kit plus extensibility.
## [2026-06-24] version | v0.10 — the AI agent as a first-class capability-layer consumer (I11), the two enabling properties on the layer, the agent online-only; opened OQ-19.
## [2026-06-25] version | v0.11 — organization as the tenant boundary (reopened I4), the operation-and-schema versioning principle (section 9.6.7); opened OQ-20.
## [2026-06-25] decision | Three sharing modes (workspace inheritance, standalone project share, workspace-settings inheritance control) all provided — closed in PRD T6.4.
## [2026-06-25] decision | Ownership is single-owner-with-admin-reclaim and no legal-weight resource is ever ownerless — closed in PRD T6.3.
## [2026-06-25] version | v0.11.1 — patch: a Scar line on each invariant I1 to I11 (the concrete bug each one prevents); no decision changed.
## [2026-07-30] decision | The coordinate reference rule is settled and now has a home: storage and interchange in SIRGAS 2000 (EPSG:4674) with the source CRS recorded, no metric ever computed in degrees, no silent drift on the render and edit round trip, no silent precision loss — closed in PRD M5.
## [2026-07-30] decision | The metric frame is chosen by the metric's purpose from a closed named set, never a global constant: geodesic on the SIRGAS 2000 ellipsoid for generic and preview figures, Sistema Geodesico Local for a certified rural parcel (NTGIR 3rd edition item 4.6, primary source), an equal-area conic on SIRGAS 2000 for the CAR and environmental area chain; UTM is explicitly not an authoritative frame for a legal area (INCRA dropped it in 2013; the SGL-versus-UTM divergence runs +0,10% to -0,14%) — closed in PRD M5 after research, superseding the geodesic-versus-UTM binary the first draft had left open.
## [2026-07-30] decision | A layer carries a storage class (element or served) that decides whether its features enter the operation queue, and an import is classified by a measured element budget — closed in PRD M2; B2's offline-import acceptance narrowed to that budget to stop contradicting foundation section 5.
## [2026-07-30] decision | The tenant is always a materialized record with a kind (personal or organization), so a freelancer and a company take the same isolation path — closed in PRD M1.
## [2026-07-30] decision | Identifiers are client-generated, opaque, stable, and never reused; the concrete variant is an ADR and an embedded device clock is never read as evidence — closed in PRD M3.
## [2026-07-30] decision | Server cursor retention is at least the maximum supported offline window, and a flush from a clientID with no cursor that starts above the first mutation number is a typed reconciliation, never an optimistic apply — closed in PRD M4, which settles the garbage-collection item T2.3 left open.
## [2026-07-30] decision | An attribute field's key is its identity and its label is presentation; user schema evolves freely while the system contracts stay additive; image, person, and date values carry references or declared instants, never bytes or ambiguous local strings — closed in PRD M6.
## [2026-07-30] decision | The legal-weight classification in force at application is recorded with the applied operation, so the trail can explain why an operation resolved the way it did — closed in PRD M7.
## [2026-07-30] decision | Every operation addresses exactly one target path, because the conflict unit is the target path; a geometry operation carries the whole geometry rather than a vertex delta so both versions stay presentable; the named multi-target exceptions (the topological arc edit) are server-side and online only — closed in PRD M9.
## [2026-07-30] decision | The four version axes (per-feature version, per-client mutation number, operation-schema version, conflict-rule version) never substitute for one another, and a client's operations apply only in contiguous mutation-number order, so a gap above the cursor is a typed resend rather than a silent skip — closed in PRD M10.
## [2026-07-30] decision | The conflict rule is a pure function over a declared contract, its geometric-predicate tolerance is declared in metres and not degrees, and inside the tolerance band a legal-weight verdict falls to flag-and-preserve — closed in PRD M13, paying the T4 debt.
## [2026-07-30] decision | The operation log is append-only and never mutated, and a legal-weight feature's current geometry is reproducible by replaying its attributed chain, which is the testable form of authorship-is-the-chain (I10) — closed in PRD M15.
## [2026-07-30] decision | The owner ratified the visual language explored in the throwaway editor prototype as Mapsift's visual identity; the prototype stays non-citable as a source of requirements and PRD section 9 (U1 to U12) is the authority that carries the identity forward — closed in PRD U-preamble.
## [2026-07-30] decision | ADR-0001 accepted: the architecture baseline the scaffold is created from (layout by unit of deploy, language roles with no Rust on the server, containerised from the first commit, configuration and secrets, generated contracts wired at scaffold, CI gates present before the code they govern, tests beside their ecosystem), plus the explicit not-yet list (apps/sync, apps/desktop, apps/mobile, the sync internals, and the dependency-gated ADRs) with the gate that unlocks each. Nothing in it depends on the OQ-10 or OQ-1 spikes, which is why it could be written first — `specs/adr/0001-architecture-baseline.md`.
## [2026-07-30] version | v0.12 — four corrections raised from an adversarial review of the canon, batched into one round: importing offline is qualified rather than forbidden (section 5: the client can read the format AND the result fits the light element path, with the surface saying which case applies before the work is lost); external data sources are a pluggable provider and no vendor is hard-coded (section 6, the decision PRD B3 had been crediting to a foundation that never made it); legal-weight marking is the tenant's rather than the workspace's (section 4, a sentence the v0.11 rekey missed); and the 15-to-25-percent cross-language figure is demoted inside its Decision block to illustration with no source and no date.
## [2026-07-30] decision | Appearance has two orthogonal axes and four shipped combinations: colour scheme (Dark or Light) and material (Glass or Solid), each a complete token set switched at the root, with per-component scheme overrides prohibited by name and Dark Glass as the default because the ratified identity is the chrome receding. Supersedes the first draft's "dark by design, a light variant only hypothetical"; the material toggle is reframed from a performance mode into a user-facing appearance mode whose performance benefit is real and secondary — closed in PRD U3 and U4.
## [2026-07-30] decision | OnPush is the v22 default and the schematic writes neither the property nor the import, so writing it by hand breaks the ADR-0002 verification rule; corrected in .claude/rules/angular.md and specs/dependencies.md, which both asserted the opposite from memory inside the very rule that exists to stop assertions from memory.
## [2026-07-30] decision | Tailwind CSS v4 is reclassified from candidate to ratified-by-the-artifact: the foundation-ratified component library depends on it structurally (class-variance-authority in 45 files, clsx in 65, twMerge as every component's composition primitive), so the open work is the mandatory reconciliation between the PRD U1 scales and Tailwind's @theme, not the choice.
## [2026-07-30] decision | Competitor names are coded in every versioned document, and the exemption that let session-handoff.md keep plain names died with the first push; the exception is now written rather than assumed: a tool named as a tool is fine in technical documentation, a competitor named as a competitor uses the code.
## [2026-07-30] decision | ADR-0003 accepted: the Angular conventions that are project decisions rather than official-style-guide restatement (every feature route lazy loaded because the initial payload already carries a renderer and a WASM core and the field tablet pays for it; signal-based data access as the default with HttpClient as the exception; one forms API per surface with an interim no-mixing rule that must be retired rather than forgotten; library imports from the barrel only). Closes the ADR-0002 section 5 violation where `.claude/rules/angular.md` decided well beyond its ADR, and records why the rest of that file needs no ADR: its authority is external and citable, and copying it in would create a second copy that drifts.
## [2026-07-30] decision | PRD gap list gained 10.6 (deferrals that name no owner and land in no section, including the three T6 requirements that defer to a permission model that is T6 itself) and 10.7 (acceptance criteria that read as tests and are not, with the explicit note that K1 and K2 are NOT fixed by inventing a number now). The index gained the ID-namespace map, and the writing rule gained its written exception for the two flat catalogs.
## [2026-07-30] decision | `specs/testing.md` written, closing the pre-requisite that CLAUDE.md and ADR-0002 both already depended on: the two-window protocol with window B forbidden from editing the tests it must satisfy, behaviour-not-implementation with the test-changes-only-when-the-requirement-changes corollary, the decision-versus-effect split (if it can only be tested with the network or a live PostGIS it was factored wrong), one shared golden corpus consumed by both runtimes with the metre tolerance and the legal-weight-falls-to-preserving case as a test, measurements recorded rather than gated in CI, and traceability by requirement ID in both directions.
## [2026-07-30] decision | `libs/ui` packaging corrected and the tree flattened from `libs/ui/mapsift-ui/` to `libs/ui/` to match the ADR-0001 layout, which also fixed the relative paths that resolved one level short; package renamed to @mapsift/ui with the Angular 22 peer dependency, the Nx `project.json` and Nx-dependent eslint config removed (no single monorepo tool spans the ecosystems), README rekeyed and pointed at PRD section 9, ZARD_ICONS became UI_ICONS, and the two raw-colour scheme overrides were removed as U1 violations. The source was audited and is a legitimate 45-primitive component library with no domain code; 11 token-based `dark:` overrides remain as design-system work under U4.
## [2026-07-30] decision | SP-1 planned (`specs/spikes/SP-1-postgres-ordered-sync.md`): two stages, the database ordering strategy with no web layer first (three candidates, per-project version against transaction-id watermark against row-version-with-CVR) and the protocol loop second, with a **negative control that implements the naive BIGSERIAL cursor first and must be caught losing rows**, because a harness that cannot catch the known bug cannot validate the fix. Delivers an ADR, the per-project write ceiling, and the settled fifth version axis (the resync cursor) in PRD M10. No further sync specification is written until it closes.
## [2026-07-30] decision | Rust boundary toolchain surveyed into dependencies.md section 2: wasm-bindgen with wasm-pack is the path and it emits the TypeScript definitions itself; Angular's WASM import **requires a zoneless application** and a per-WASM type definition, which turns zoneless from a default into a hard requirement and gives a concrete reason to go through wasm-pack; the feature rides an active standards proposal, so the WASM load is isolated behind one thin adapter in apps/web; the client geometry engine is the pure-Rust `geo` crate because the `geos` binding links to a system GEOS that does not exist in a browser, which makes the PRD M13 tolerance structural rather than incidental; **and the Rust-to-Dart type generation has no tool** (Specta, ts-rs and Typeshare all stop short of Dart), so it stays open and must not be planned as solved.
## [2026-07-30] decision | `specs/dependencies.md` written: the dependency survey, harvesting what was already verified this round (Angular v22 and its schematic and test-builder behaviour, MapLibre GL JS v5 requiring WebGL2, ST_TransformPipeline needing PostGIS 3.4+ with the axis-normalisation caveat, the Postgres sequence trap that blocks a safe resync cursor, WCAG 2.2 AA, the 50 ms and 200 ms interaction thresholds, NTGIR 3rd edition). Fixes the ratified-versus-pinned distinction (only a lockfile pins), requires a verification date on every claim, and carries the fourteen dependency-gated ADRs plus the SP-1 sync-strategy spike.
## [2026-07-30] decision | ADR-0002 accepted: code layout and generation. CLI-first generation in every stack (a hand-written framework file reproduces a remembered older version; the generator writes what the installed version produces, verified by a --dry-run diff); the Angular component file layout at the CLI default with a three-condition inline exception (at most 5 lines AND no control flow AND no bindings beyond content projection); one component per folder; feature-not-type organisation with a countable 8-direct-children split; naming from the installed schematic. Fixes the three-level split: the ADR decides, a path-scoped .claude/rules/*.md enforces, a per-stack CLAUDE.md carries only operational residue after the scaffold, because a subdirectory CLAUDE.md loads on demand and is not re-injected after compaction — `specs/adr/0002-code-layout-and-generation.md`.
## [2026-07-30] decision | ADR conventions set in ADR-0001: `specs/adr/NNNN-kebab-title.md`, Context/Decision/Consequences, an accepted ADR is superseded by a later one rather than edited (patch-only corrections allowed).
## [2026-07-30] decision | The project is containerised from the first commit, development and deployment alike, with third-party services as compose units under infra/, named volumes for dependency and build artifacts, and libs/core building its WASM artifact in its own stage so the web build needs no host Rust toolchain; the owner's rationale is reproduction across machines, VMs, and CI, and parity between development and deployment — closed in ADR-0001 section 3.
## [2026-07-30] decision | For design-system and interface work only, the prototype is a permitted visual reference and is expected to be opened, under three rules: always recreate by refactoring and never copy a file, class, or structure; where it and PRD section 9 disagree, section 9 wins and the look is preserved by other means; and the permission covers appearance and interaction feel only, never architecture, state, persistence, identity, or logic. A visual comparison is legitimate evidence of a match and is the only way its code may be cited — closed in PRD section 9 preamble.
## [2026-07-30] decision | The design system is a closed token system (radius in 4 steps, type in 6, text in 4, one accent, a closed spacing scale) with no raw literals in components — closed in PRD U1.
## [2026-07-30] decision | One glass material, three layers differing only by alpha and elevation (chrome, floating surface, transient overlay), with inset content solid and never blurred; a surface that invents its own recipe is a defect — closed in PRD U2.
## [2026-07-30] decision | Performance mode removes the filter entirely rather than blurring by zero, is applied at the document root so it reaches overlays outside the app root, and the N1 budgets are measured in both modes — closed in PRD U3.
## [2026-07-30] decision | Interactive states are semantic tokens and never colour-named utilities, which is the maintainability fix the prototype's hover naming demanded — closed in PRD U9.
## [2026-07-30] decision | The chrome is built from @mapsift/ui by package name, with no relative import into its source and no bespoke duplicate of a library primitive; a shared primitive moves into the library rather than being copied — closed in PRD U10.
## [2026-07-30] decision | The design tokens are exported in a platform-neutral form and are the cross-platform contract, since the Angular library does not cross to Flutter but the identity must — closed in PRD U12.
## [2026-07-30] decision | Three distinct performance budgets, never conflated (the I6 per-tile render budget on the served path, the editable working-set budget on the element path, the element budget at classification), made testable by the standard interaction rules (no main-thread task over 50 ms, no interaction over 200 ms at the 75th percentile) and recorded with device, versions, fixture, and date — closed in PRD N1.
## [2026-07-30] decision | Tenant isolation is covered by construction: a test enumerates every tenant-owned table and asserts row-level security enabled and forced, so a new table without the policy fails CI, and the defeat conditions (table owner without FORCE, a bypass role, the tile role connecting privileged) are explicit test cases — closed in PRD N2.
## [2026-07-30] decision | No third-party code executes inside Mapsift while OQ-14 is open, because shipping an extension path before its sandbox is decided is how a permission model gets retrofitted onto running code — closed in PRD N4.
## [2026-07-30] decision | No compliance claim appears anywhere in the product or its material while OQ-16 is open, and a deletion request is honoured everywhere except the legal-weight trail with the exception stated to the person — closed in PRD N5.
## [2026-07-30] decision | No requirement may assume the device is trusted; server-side credential revocation blocks that installation's flush and renewal, and the honest limit (it does not protect data already at rest, which is why OQ-17 is open) is stated rather than implied away — closed in PRD N6.
## [2026-07-30] decision | WCAG 2.2 level AA is the declared accessibility target, with the WebGL map canvas treated honestly through a declared non-visual path (the attribute table and inspection) rather than a pretense of an accessible canvas — closed in PRD N7.
## [2026-07-30] decision | A large offline queue flushes in bounded batches with per-batch acknowledgment, preserving mutation-number contiguity, so a long trip never becomes one all-or-nothing request — closed in PRD N10.
## [2026-07-30] decision | WebGL2 is the web floor because MapLibre GL JS v5 removed WebGL1, and an environment below the floor is told plainly rather than shown a broken map — closed in PRD N11.
## [2026-07-30] decision | Capability parity across surfaces is the default and every difference is declared with a reason from a closed set (needs the server, needs absent hardware, needs an absent input modality, gated by an open question); convenience and delivery order are not reasons — closed in PRD S1.
## [2026-07-30] decision | A captured vertex records its capture method and precision estimate, and a geometry whose vertices do not meet the accuracy the applicable regime requires is never presentable as satisfying it (NTGIR 3rd edition item 4.4: 0,50 m artificial, 3,00 m natural, 7,50 m inaccessible), so device positioning cannot produce certification-grade cadastral geometry — closed in PRD S5.
## [2026-07-30] decision | Desktop offline stays session-scoped like web until OQ-9 lands, and no desktop affordance may imply whole-project offline before it — closed in PRD S3.
## [2026-07-30] decision | Every surface can present a blocking force-upgrade state and never renders correctness-relevant state it cannot interpret, which is what makes the T9.4 typed force-upgrade real rather than a dead end — closed in PRD S9.
## [2026-07-30] decision | Contracts are generated in two directions (OpenAPI to TypeScript and Dart, Rust core types to TypeScript and Dart) with CI failing on stale output; the conflict rule's two implementations are the one deliberate duplication, guarded by golden tests and never by generation — closed in PRD M12.
## [2026-07-31] version | v0.13 — regulatory content is per-jurisdiction data and never code (four kinds: which feature types carry legal weight, regulatory geometry parameters, deliverable and attestation templates, retention), with a jurisdiction-neutral criterion for legal weight so packages differ in their answers and never in their question; OQ-16 reframed from an LGPD posture to a multi-regime privacy posture designed against the strictest regime served; OQ-20's retention half corrected, since "retain indefinitely" is not a lawful default everywhere, with the legal-weight chain's author pseudonymizable so an erasure request is honoured while the chain survives as evidence; opened OQ-21 (the shipped desktop and mobile builds as software placed on a market).
## [2026-07-31] decision | NORM CORRECTION, the load-bearing half of the round: the canon cited a revoked standard inside the requirement that computes legally consequential area. Portaria INCRA 629 of 05/04/2022 art. 3 revoked the NTGIR 3rd edition and the Manual Tecnico de Posicionamento that PRD M5 named as required reading; the live standard is the MTGIR 2nd edition (Portaria 2.502 of 22/12/2022). Content intact and renumbered: precision 1.4.4, area in the local frame 1.4.6 cross-referring to 3.8.1, Gauss in hectares 3.8.3, azimuth by Puissant 3.8.5. Propagated to PRD M5 and S5, CLAUDE.md, the reference catalog and the dependency survey — verified 2026-07-31, record in `domain-questions.md` section V.1.
## [2026-07-31] decision | PRD M5's open item on the local-frame origin CLOSED by the live norm rather than by an ADR: MTGIR 2nd edition item 3.8.1(a) fixes the origin, for computing area, as the mean of the coordinates of the parcel in question, so the practice the literature reported is the written rule and the chosen-vertex reading is settled. What remains is that the ADR implements that origin and invents no other.
## [2026-07-31] decision | Legal weight follows the nature of the feature and never the state of an external registry: a registration status is recorded, displayed, exported and used for workflow warnings as provenance, and never raises or lowers the protection, and protection is never removed retroactively. Overrules the reference catalog's microdecision, which had proposed gating on an active status — closed in PRD M7, on the facts that a status is assigned before any analysis, that a record under challenge is when a boundary error costs most, and that a protection switching with slowly-synchronised external state opens the window the product exists to close.
## [2026-07-31] decision | A feature that becomes legal-weight after being born a sketch keeps its prior history as recorded, the reclassification is itself a dated attributed operation, and the reinforced protection applies forward with no retroactive reclassification — closed in PRD M7.
## [2026-07-31] decision | Jurisdiction packages: every regulatory rule is a dated, sourced, versioned data entry bound to the project that resolved against it, so re-reading an old project reproduces the rule in force rather than today's, and a second jurisdiction adds a package and edits no engine — closed in PRD M16, implementing the foundation v0.13 decision.
## [2026-07-31] decision | Five capabilities promoted into the native analysis core on frequency and centrality and never on parity: change detection between two classifications with its class-by-class cross-tabulation (the operation that defines the anchor domain and that the core had omitted), slope with aspect and hillshade from the same kernel (inclination bands define legal categories, so every property diagnosis passes through it), union (completing an overlay family the core carried in half), and reprojection exposed as an invokable tool — closed in PRD G2.
## [2026-07-31] decision | J2 closed and with it the last non-falsifiable acceptance criterion in the PRD: the deliverable is a signed document plus a receiving-body data package, both generated from the project, every figure the authoritative M5 value, and the test is that editing one vertex and regenerating leaves no stale number anywhere — closed in PRD J2, with export presets as jurisdiction-package entries in J1.
## [2026-07-31] decision | The accessibility target stays at WCAG 2.2 AA; a research round proposed adopting the older version that the European procurement standard references, and the proposal was rejected because it lowers an existing commitment. The procurement standard is recorded as the reference a contract names — closed in PRD N7.
## [2026-07-31] decision | Verification round over the domain-and-compliance answer round, recorded in `domain-questions.md` section V: nine claims graded against primary sources, seven confirmed (two with corrections to the answering round), one confirmed with an access caveat, and one REJECTED and kept out of the canon (the consolidated-area recomposition ladder, whose primary text could not be reached and whose summary was imprecise on both the band above four fiscal modules and on which provisions were vetoed). The rule the round proves: a report that self-approves is where to dig, and only what is graded enters.
## [2026-07-31] version | v0.14 — the ratified database major moves from PostgreSQL 16 to 18. The request was to pin "the LTS version" and the premise is false: PostgreSQL designates no long-term-support version, supporting every major for five years from initial release, shipping a major yearly and a minor quarterly, and recommending you always run the current minor. So the pick is by remaining runway, roughly two years on 16 against roughly four on 18, and a closed-scope product built to completion and then run for years gains nothing from ratifying a half-spent major. The major is ratified and the minor always runs current. Ecosystem checked and non-binding: PostGIS 3.6 covers 12 through 18, Django's floor is 14. The next major is in beta and excluded. Verified 2026-07-31 against the upstream versioning policy and release list.
## [2026-07-31] decision | Decide the version before the measurement, not after: the SP-1 harness was already up on the prior major and was switched before producing a single number, because every measurement is recorded with its versions (PRD N1) and measuring on a major the product will not ship produces numbers describing an abandoned version.
## [2026-07-31] version | v0.15 — performance is engineered, and the established technique is researched before an implementation is settled for, on the same discipline the external-dependency rule applies to versions. Two classes treated differently: structural performance is free at design time and therefore not optional (the index the query needs, the batch that replaces a loop of round trips, the critical section held for one statement instead of one transaction, the payload that crosses a boundary once), and skipping one is a defect with a performance symptom; complexity-adding optimisation is bought with a measurement and never a hunch, generalising the tiling gate of section 6. The commercial reasoning is on the record: the reference tools in this market are not fast, a professional pays for every wait across a working day, and an advantage made of hundreds of small decisions is one a competitor cannot answer in one release. Also closes OQ-10 and lifts the rule that blocked specification on the sync path.
## [2026-07-31] decision | ADR-0004 accepted: the sync ordering strategy is the per-project version, allocated inside the flush transaction so version order is commit order by construction. Chosen on FAILURE MODE rather than throughput: its contention is local, proportional and diagnosable, while the transaction-id watermark fails non-locally twice (any unrelated writing transaction stops the feed for every user, measured at a full 4 s stall; and its cursor is bound to the physical identity of the cluster, so a logical restore silently invalidates every offline client's cursor, which matters because Mapsift's cursor lives on field devices for weeks). Row-versioning with a client view record was eliminated on read cost (2,615 rows scanned per poll at a 5,000 backlog, halving write throughput). Delivers the SP-1 exit — `specs/adr/0004-sync-ordering-strategy.md`.
## [2026-07-31] decision | Two engineering rules are part of the ADR-0004 decision rather than optimisations to add later, because serialisation is caused by how long the lock is held rather than by the number of writers: allocate the whole version range in ONE statement per flush, and take the allocation LAST, so validation, dedup, contiguity, feature versions and the conflict rule all run outside the critical section. Measured with ten people editing while a client flushes 3,000 operations: worst interactive save falls from 782 ms to 40 ms and the flush itself gets four times faster. LATE matters more as pushes get heavier, since the critical section stops growing with them.
## [2026-07-31] decision | PRD M10 is complete: the resync cursor is a FIFTH version axis, the per-project version, rather than an existing axis doing double duty. The requirement had declared itself incomplete until SP-1 closed — closed in PRD M10 v0.11 on ADR-0004.
## [2026-07-31] decision | Two documented costs of the ratified strategy do not apply to Mapsift and the reason is recorded so nobody reopens it: soft deletes are unnecessary because the log is append-only (M15) so a delete is an appended operation, confirmed by the spike passing the delete criterion on all three candidates; and partial-sync/read-authorisation difficulty does not arise because access is granted per whole project (T6.4, T6.5). The one cost that does apply and is accepted: no atomicity across projects — recorded in ADR-0004.
## [2026-08-01] decision | Backend dependency survey run in one round, closing the four rows `dependencies.md` carried as "ratified choice, no surveyed version" and three questions nobody had noticed the scaffold forces. Versions found and verified against primary sources: django-ninja 1.6.2 (declaring Django >=3.1,<6.1 and pydantic >=2.0,<3.0), Pydantic 2.13.4, Celery 5.6.3, Channels 4.3.2, psycopg 3.3.4, mypy 2.3, django-stubs 6.0.7 (with a 5.2 line), ruff 0.16.1, pytest 9.1.1, pytest-django 4.12.0, pytest-asyncio 0.26.0 (date unconfirmed and recorded as such), uv 0.12.1. Nothing pinned, because only a lockfile pins.
## [2026-08-01] decision | The Django release inside the ratified 5 line is **5.2 LTS**, security-supported to 2028-04-30, with one planned LTS-to-LTS migration to 6.2 LTS after it ships in April 2027. Chosen on how forced the next hop is rather than on features: both paths converge on 6.2 LTS, but 5.2 leaves twelve months of slack to migrate under green tests while 6.0 (EOL 2027-04-30) leaves none and routes through 6.1, which django-ninja does not accept. The v0.14 PostgreSQL arithmetic deliberately did NOT transfer, because a Django upgrade is not a dump-and-reload event and Django ships a major every eight months, so the strategy is to follow the LTS line rather than to maximise a single major's runway. **No foundation round: 5.2 is inside "Django 5", so this is a pin.** Cost recorded rather than hidden: 5.2's mainstream window closed 2025-12-03, so it gets security and data-loss fixes only.
## [2026-08-01] decision | The interpreter is **Python 3.13** (supported to 2029-10-31) and not 3.14, on a single binding constraint: Celery's own documentation lists supported CPython as 3.9 through 3.13 while its 5.6 release notes claim initial 3.14 support, and initial support the supported-versions list has not caught up with is not where the heavy-analysis job queue goes. Everything else in the backend accepts 3.14 (Django 5.2 added it in 5.2.8). Re-check trigger written down: when Celery's supported list names 3.14, revisit, most naturally in the same round as the 6.2 LTS move.
## [2026-08-01] decision | **uv** is the Python packaging and lock tool, closing a choice that had never been made anywhere (ADR-0001 said only "a Python packaging and lock tool", CLAUDE.md said "uv or poetry", the survey had no row) while `.claude/commands/quality-gate.md` had already started writing `uv run`, which was tooling deciding what its authority left open. Four project-specific reasons: it manages the interpreter itself so the 3.13 pin is enforced by the resolver rather than by convention; its speed lands on the containerised loop ADR-0001 named as carrying real friction, which the foundation section 10 performance rule counts as structural; its lockfile is universal across platforms, which matters when developer, container and CI are not the same environment; and it is Astral's like the already-ratified ruff. Cost: still pre-1.0 at 0.12.1, so pinned exactly and moved deliberately, the same discipline ruff gets.
## [2026-08-01] decision | The type-checking pairing is the real cost of the LTS and is settled empirically at scaffold rather than guessed: django-stubs tracks the Django release it targets, so django-stubs 5.2.x gives full fidelity for Django 5.2 and caps mypy at 1.19, while django-stubs 6.0.7 allows mypy up to 2.3 and supports 5.2 only partially. Order at scaffold: try 6.0.7 with mypy 2.3 first, fall back to 5.2.x with mypy 1.19 if partial support produces false positives, record which won with its date. Matters because mypy 2.0 is a real break rather than a renumber (`--local-partial-types` and `--strict-bytes` as defaults, which a --strict codebase wants, plus `--num-workers` parallel checking, which the CI gate wants). The tension dissolves at the 6.2 LTS migration.
## [2026-08-01] decision | Redis leaves the sync correctness path: its `dependencies.md` row no longer carries the Client View Record possibility, because SP-1 eliminated the row-version strategy on read cost and ADR-0004 ratified the per-project version, whose cursor is an ordinary integer column in PostgreSQL. Redis is the Channels layer and the Celery broker, and nothing on the correctness path.
## [2026-08-01] decision | `.gitignore` completed before the first install of any ecosystem, from 4 entries to a sectioned file in which every entry carries its reason. The governing rule written into the file: ignore what a tool regenerates, never ignore what pins a decision, since `dependencies.md` holds that only a lockfile pins and a lockfile outside version control would mean nothing is pinned. Verified rather than assumed: no already-tracked file became ignored, every intended pattern catches across all four ecosystems, and every lockfile and version pin stays trackable. Carries an explicit never-ignore list (uv.lock, package-lock.json, Cargo.lock, .python-version, the .env example templates, specs/**, and the shared conflict-rule golden corpus, whose absence would leave C10 with nothing to compare against).
## [2026-08-01] decision | Two questions the `.gitignore` surfaced were flagged rather than decided by omission, because a tooling file deciding what its authority left open is the drift the governance rule forbids: whether the generated cross-language contracts are committed or built reproducibly is ADR-0001 section 5's own either-or and stays there, and how the large geospatial fixture corpus is stored becomes item 15 of the `dependencies.md` ADR agenda. Nothing in `.gitignore` excludes fixture data, deliberately, because a silently ignored fixture is a suite that passes on one machine and fails on another.
## [2026-08-01] decision | The forge is GitHub and `origin` is set, closing the last item before the scaffold. The history was reset deliberately before the first push, because earlier commits carried files that should not sit in a permanent record and doing that at two commits costs nothing while doing it after the scaffold is surgery. ADR-0001 section 6's CI gates now have somewhere to run, which was the real reason the remote sat in the queue rather than in a footnote. The presumption the tooling already carried (dev-workflow waiting on `gh pr checks`, the github-workflow agent, the `/pr` command) is ratified rather than left floating.
## [2026-08-01] decision | A README.md was written as the public face of a repository whose product stays private until maturity: it names no competitor and uses no MC code either, because a code in a public README is unreadable to a newcomer and still announces that a coded competitor list exists; it declares no license, since OQ-7 is open and the project moved from open-source-on-day-one to private-until-mature precisely so that decision is taken once with evidence; and it carries three sections marked pending (running it, building for production, concrete host requirements) to be filled in the same pass that creates the Docker and compose setup.
## [2026-08-01] decision | Three files in `specs/` became internal and untracked: the market research as before, plus `session-handoff.md` and `domain-questions.md`, which are working material rather than contract. Fan-out done the same day: the `.gitignore` never-ignore block stopped claiming `specs/**` wholesale and now lists the three exceptions with their reason, and `specs/index.md`, which is tracked and public, marks both newly-internal files so it stops describing documents a reader cannot open.
## [2026-08-01] decision | The `apps/api` scaffold landed, generated with the CLI at every step (uv init, uv add, uv python pin, django-admin startproject) and never hand-written, per ADR-0002 section 1. Layout: `apps/api` is a standalone uv project with its own pyproject.toml, uv.lock and .python-version, so the folder can be cut out whole, which is the ADR-0001 rule that keeps a future split cheap. The gates were configured BEFORE the code they govern, which is ADR-0001 section 6's own ordering, and the first behaviour written was covered by a test written before it.
## [2026-08-01] decision | The django-stubs and mypy pairing question that the survey deferred to scaffold time is ANSWERED and the preferred branch won: django-stubs 6.0.7 with mypy 2.3.0 against Django 5.2.16 gives `Success: no issues found`, so the fallback to the 5.2 stub line with mypy 1.19 is not needed and the codebase gets the LTS and the current type checker at once. Two mypy plugins are required together and neither project's documentation says so: mypy_django_plugin teaches the ORM and the settings, pydantic.mypy teaches that a model coerces its input, and without the second every correct settings construction is an error.
## [2026-08-01] decision | The type checker loads Django settings, so an incomplete environment surfaces as `INTERNAL ERROR -- Error constructing plugin instance of NewSemanalDjangoPlugin`, which reads like a broken mypy and is nothing of the sort. Same family: pytest-django sets Django up inside pytest_load_initial_conftests, which runs before any conftest.py, so a conftest cannot supply the variables in time. Fixed by wrapping the failure in ImproperlyConfigured with a message naming .env.example, and by leaving DJANGO_SETTINGS_MODULE out of the pytest config while every test is a pure decision that needs neither Django nor a database.
## [2026-08-01] decision | Three smaller traps recorded with tests rather than comments: PostgresDsn rejects `postgis://` (PostGIS is the Django ENGINE, the URL scheme stays postgresql://), PostgresDsn is a multi-host URL so credentials live in hosts()[0] and reading them off the URL raises AttributeError at runtime, and a list field needs NoDecode because pydantic-settings JSON-decodes a raw environment value before any validator runs, a trap that only appears on the real environment-variable path.
## [2026-08-01] decision | `django.contrib.gis` stays out of INSTALLED_APPS until the first geometry model, while the database ENGINE is the PostGIS backend from the first line. The app loads GDAL and GEOS at import and the developer host is not required to carry them (ADR-0001 section 3 puts running in the container and authoring on the host); verified that manage.py check passes on a host with no gdal-config at all. Also recorded: Celery and Channels are ratified and deliberately not installed until code imports them, because a dependency nothing imports is a ghost that still has to be upgraded and audited.
## [2026-08-01] decision | The Angular workspace was created for `libs/ui`, generated with the CURRENT CLI (22.1.2, against the 22.0.0 installed globally) into a scratch directory and then integrated by hand, because `ng new` at the repository root would have overwritten the README.md and the .gitignore. Every workspace-level shape was copied from what the generator wrote rather than authored: the angular.json project entry, the ng-packagr and unit-test builders, and the tsconfig references were all read off a throwaway `ng generate library` run first. Resolved: @angular/core 22.1.0, CLI and build 22.1.2, ng-packagr 22.1.0, TypeScript 6.0.2, CDK 22.1.0, Vitest 4.0.8, angular-eslint 22.1.0.
## [2026-08-01] decision | **TypeScript 6 turns `strict` on by default, which is why the Angular schematic no longer writes it into tsconfig.json.** Verified empirically rather than assumed: the generated config with no explicit `strict` rejects an implicit `any` and a null assignment identically to `--strict` forced. C5 is therefore satisfied by the language default, and the hazard to record is the inverse: pinning TypeScript below 6 would silently turn strict OFF across the whole front end, and nothing in the config would show it.
## [2026-08-01] decision | lucide-angular is incompatible with Angular 22 in EVERY published version (peer pinned at 13.x-21.x, including its 1.0.0), so the library's icon module moved to @ng-icons/core and @ng-icons/lucide, which declare peer >=21 and which the ratified editor prototype already runs on Angular 22. The swap keeps the same Lucide artwork and the same public API of `ui-icon`, and all 89 icon names were verified against the installed package's real exports rather than translated by naming convention; the one custom icon (`dark-mode`) was rewritten as an SVG string. `uiAbsoluteStrokeWidth` was dropped because ng-icons has no equivalent and no consumer used it.
## [2026-08-01] decision | Two real defects were found in the vendored library by the Angular 22 compiler and the linter, and both are fixed with a regression test: `UiPaginationComponent` declared `uiPageIndexChange` explicitly while `uiPageIndex` is already a `model` that emits it, so every page change reached the consumer TWICE (NG1054); and the carousel plugin loader threw a fresh Error in three catch blocks without attaching the original as `cause`, which discards exactly the information PRD N9 requires a failure to be reconstructible from. Also corrected: a `reduce` that mutated its accumulator, a dead initialiser in the slider keyboard handler, and two InjectionTokens still carrying the `Z_` prefix of the upstream fork that the 2026-07-30 rename missed.
## [2026-08-01] decision | The library's lint configuration was aligned to conventions it deliberately uses rather than the code being bent to a generated default: a component may be an element OR an attribute and a directive attribute is kebab-case (the shadcn pattern of applying a primitive to a native element, which is what keeps the native semantics and accessibility), an underscore prefix marks an intentionally unused parameter (most of the ControlValueAccessor surface), and `aria-label` and `disabled` are allowed input aliases because a library that wraps native elements must expose the native names. What could NOT be configured honestly is enumerated instead: four directives still on a camelCase selector are listed by file in `libs/ui/eslint.config.js`, so the debt is countable and a fifth cannot be added without editing that list.
## [2026-08-01] decision | `libs/ui/tsconfig.json` was deleted rather than repointed. It was the root tsconfig of the standalone workspace the library came from, and it extended `../../tsconfig.base.json`, an Nx-convention filename that never existed here. The generated layout has no such file: the three children extend the workspace root directly, which is what they now do. Known debt recorded and NOT fixed here, because it is design-system work under PRD section 9: 69 components use an inline template against ADR-0002 section 2, four directive selectors are unmigrated, and the library has near-zero test coverage (the three tests added here are the first).
## [2026-08-01] decision | `libs/core` scaffolded, generated with the toolchain at every step (cargo init, cargo add, cargo remove) per the path-scoped rule that a Cargo.toml stanza is never hand-written from memory. Rust 1.95, edition 2024, crate-type `["cdylib", "rlib"]` because the two serve different consumers: cdylib is what wasm-pack compiles for the web and desktop clients and later for the Flutter FFI library, rlib is what `cargo test` links so the pure logic is testable on the host with no browser in the loop. Pinned: geo 0.33.1, wasm-bindgen 0.2.126. Release profile carries `opt-level = "s"` and LTO because the artifact is downloaded by a field tablet on a bad connection (PRD S4). No `repository` or `license` key, deliberately, because the licensing posture is OQ-7 and open.
## [2026-08-01] decision | The PRD M12 contract path is PROVEN end to end rather than assumed: `wasm-pack build --target web` produces pkg/ with the wasm binary, the JavaScript glue and **TypeScript definitions generated from the Rust types**, doc comments carried across, at 41 KB. This is the Rust half of the generated-contract rule, and it confirms what `specs/dependencies.md` section 2 recorded from the survey. pkg/ stays out of version control, as that section requires and as `.gitignore` enforces.
## [2026-08-01] decision | **`geodesic_area_unsigned` is a trap and the test caught it before any code depended on it.** "Unsigned" does not mean the absolute value of the signed area: it means the area the ring encloses UNDER ITS OWN ORIENTATION, and a clockwise ring on a closed surface encloses the rest of the planet. Measured on a one-degree square: 1.23e10 square metres counter-clockwise against 5.10e14 clockwise, which is the surface of the Earth minus the square. In a product that computes legally consequential areas, a parcel whose ring happens to be reversed would have reported a number that is enormous, wrong, and entirely plausible on a screen. The correct call is `geodesic_area_signed().abs()`, and the regression test now pins the specific failure mode rather than only asserting equality.
## [2026-08-01] decision | The client core computes exactly ONE metric frame, geodesic, which M5 names as the frame of every client preview; the Sistema Geodesico Local and the equal-area conic produce legally consequential numbers, are authoritative, and therefore live in PostGIS on the server. Enforced by the type system rather than by review: `MetricAuthority` has a single variant, `ClientPreview`, so this crate cannot construct an authoritative metric even by mistake, and `MetricUnit` has no degree variant at all. Recorded with it: the ready-made WGS84 geodesic constant is used rather than a custom GRS80 one, because SIRGAS 2000's ellipsoid differs only in the ninth significant digit of the inverse flattening (sub-millimetre over a thousand kilometres, orders of magnitude below the metre-denominated tolerance of M13), every value here is a preview by construction, and a custom ellipsoid could only be threaded through the distance path, leaving distance, area and perimeter disagreeing about the shape of the Earth.
## [2026-08-01] decision | `apps/web` scaffolded with `ng generate application` at the current CLI, zoneless (the default since v21 and a HARD requirement of the WASM integration path, not a preference) and with `--style=tailwind`, which wires Tailwind 4.1.12 through PostCSS. That is not a new choice: `specs/dependencies.md` records Tailwind v4 as ratified by the artifact, since the component library depends on it structurally, and 4.1.12 is the exact version the ratified prototype ran.
## [2026-08-01] decision | The WebAssembly core is consumed through an injected contract (`MAPSIFT_CORE`) rather than a direct import, and the discovery that forced it is worth keeping: the core is fetched at module load, jsdom does not implement that fetch, so **every spec that merely imported a consuming component failed**, including one that never touched the core. That is the effect leaking through the boundary, and `specs/testing.md` section 3 already prescribes the fix: effects sit behind a narrow interface with a real adapter and a test fake. Component tests now provide a fake and load no WebAssembly at all; only `wasm-mapsift-core.ts` imports the module, so a change in how it loads lands in one file.
## [2026-08-01] decision | The WASM crossing is proven by an integration test that needs a real browser, split into its own `test-browser` target (`**/*.browser.spec.ts`, excluded from the default run) rather than deleted or faked green. It does not execute yet: the browsers option requires one of `@vitest/browser-playwright`, `@vitest/browser-webdriverio` or `@vitest/browser-preview`, and a new dependency is never added on an agent's initiative. **Recorded as pending rather than silently skipped**, because a suite that reports green while proving nothing is worse than one that says what it did not run. Google Chrome is present on the development machine, so only the driver package is missing.
## [2026-08-01] decision | Comment discipline settled and written into both the global directive and `CLAUDE.md`: an inline comment earns its place only as a TRAP (the correct code looks wrong, or the wrong code looks right, so without the note someone "fixes" it and reintroduces the defect), documentation on a public API is welcome at one to three lines, and reasoning is never restated from the canon but referenced by identifier. The project-specific argument is the load-bearing one: this repository has a fan-out rule whose purpose is to stop a decision existing as two copies that drift, and reasoning copied into a comment is exactly that second copy, outside the fan-out. Applied as hygiene across everything written this session: comment and doc density fell from 39% to 13%, 236 lines removed against 72 added.
## [2026-08-03] decision | `infra/` landed and ADR-0001 section 3 is real rather than declared: one development compose file (PostgreSQL 18.4 with PostGIS 3.6, the API, the web client, and the Rust core as a toolchain rather than a service), two Dockerfiles, a root `justfile`, and the CI workflow carrying the section 6 gates. Verified rather than asserted: every gate runs green inside the containers, `migrate` applies against PostGIS through the GeoDjango backend, the API answers on `/api/health` and `/api/docs`, and the web dev server serves the Angular shell.
## [2026-08-03] decision | CI runs the gates INSIDE the same images the developer runs, built from the same Dockerfiles, and deliberately does not use `infra/compose.yaml`: that file bind-mounts the source and runs as uid 1000, neither of which is true of a runner. The parity ADR-0001 section 3 asks for is parity of ENVIRONMENT, which is the image; the mount strategy is a development-loop detail. The whole workflow was simulated locally before being written to disk, and all twelve steps pass.
## [2026-08-03] decision | Redis and object storage are ratified and deliberately ABSENT from compose, on the same rule that kept Celery and Channels out of the lockfile: nothing imports them, and a service nobody connects to is a container nobody notices breaking. The tile servers are absent for a different reason, which is that the choice is an open ADR and ADR-0001 section 8 forbids scaffolding a decision that has not been taken. Both land with the code that uses them.
## [2026-08-03] decision | `infra/compose.prod.yaml` was NOT written, and the absence is the decision. The deployment compose file would have to assume three things nobody has decided: which application server serves Django (nothing in the lockfile does, and a dependency nothing imports is still one to upgrade and audit), where it deploys, and what terminates TLS. ADR-0001 section 3's rule that deployment is a separate compose file differing only in configuration and build target stands untouched; the artifact waits on a deployment target existing. Recorded in the README where a reader looks for it.
## [2026-08-03] trap | PostgreSQL 18 moved the container data directory: the path every older tutorial gives, `/var/lib/postgresql/data`, still mounts happily on an 18 image and simply is not where the cluster lives, so the database silently starts empty on every recreate. The volume mounts `/var/lib/postgresql`.
## [2026-08-03] trap | A named volume over a path the image does not contain is created as a ROOT-OWNED mount point, so a container running as a non-root user cannot write to it. Docker seeds a volume from the image only when the path already exists. It bit `CARGO_HOME/registry`, which a fresh Rust image does not have until something fetches a crate, and the symptom was `cargo metadata` failing on a permission error that reads like a corrupt registry. Fixed by creating both the registry and the target directory in the image before the chown.
## [2026-08-03] finding | The developer host runs uv 0.11.17 while `dependencies.md` ratifies 0.12.1. The image pins the surveyed version, and `uv lock --check` was run against `apps/api/uv.lock` under 0.12.1 before pinning, so the lockfile is confirmed current rather than assumed. The host being behind its own survey is worth a `uv self update`; it is also the argument for the container being the source of truth for running.
## [2026-08-03] decision | The published database port defaults to 5433 rather than 5432, because the API reaches the database over the compose network and the published port exists only for a psql session or a GUI from the host, where a system PostgreSQL usually already holds 5432. This machine has one, so the default that looks right would fail on the first `up` with a bind error.
## [2026-08-03] trap | **The Angular CLI prompted for usage analytics inside the container, the prompt was answered, and it wrote a tracking identifier into `angular.json`, which is TRACKED.** One `just check` away from a public repository carrying a per-project analytics UUID that every developer and every CI run would report from. Reverted before any commit, and prevented structurally rather than by vigilance: `NG_CLI_ANALYTICS=false` is an ENV in the client image, so no invocation from any container can ask again.
## [2026-08-03] trap | **Two containers must not share the Angular persistent cache directory.** It is an LMDB store whose readers coordinate through a process-shared mutex in shared memory, and two containers do not share an IPC namespace, so a gate run against the same `.angular` volume while the dev server holds it dies on `TypeError: Cannot read properties of null (reading 'refCount')` followed by an esbuild deadlock. Diagnosed rather than guessed: dev server down, LMDB passes three times; dev server up, the same command fails every time; and the first successful run only passed because the stack was not up yet.
## [2026-08-03] decision | The fix for that is `CI=1` on every one-shot run, which is what Angular reads to disable the persistent cache, and it is in one place in the `justfile` rather than per recipe. Two alternatives were tried and rejected with evidence. `NG_BUILD_CACHE_STORE=sqlite` (Angular 22 supports it) removes the crash and replaces it with `"contents" must be a string or a Uint8Array` from the cached load result, so it trades a crash for a corrupted cache. An isolated cache volume works, and it was **measured against no cache at all: 6s cold and 4s warm against 5s and 6s**, so about two seconds, which does not buy an extra volume outside compose's lifecycle threaded through every gate recipe.
## [2026-08-03] decision | `just contracts` does not run a tree-wide `git diff --exit-code`, and the `/quality-gate` command was corrected to match. With no generated artifact tracked, that diff fails on ordinary work in progress rather than on a stale contract, which is a gate that cries wolf. What it asserts today is that wasm-pack regenerated the definitions; the scoped diff lands the day a generated file is committed, which is ADR-0001 section 5's open either-or.
## [2026-08-03] decision | **Foundation v0.16: observability and availability go on the record, split by the same rule section 10 already uses for performance.** Structural now, because none of it retrofits cheaply: logs structured and carrying their correlation keys from the first line of code (the PRD's reconstruction requirement is a join, and a join cannot be added to free text later), redaction as a property of the logging path rather than of each caller's diligence, telemetry emitted vendor-neutral with a swappable backend, liveness and readiness as different probes, degradation announced rather than silent, and a backup that counts only once a restore has been rehearsed and recorded with its date and its numbers. Deferred with triggers rather than shrugs: the telemetry backend is an ADR triggered by the FIRST REAL USERS, and the availability target and the replica topology wait for a measured need.
## [2026-08-03] finding | **The OpenTelemetry Python logs SDK was still listed as Development in May 2026** while its traces and metrics SDKs are stable, and while the logs signal itself is stable in the specification and in Java, .NET, C++ and PHP. That dated fact decides the mechanism and not the property: the log path runs through the standard library with trace identifiers injected, so the record a compliance question is answered from does not depend on an unstable SDK. The re-check trigger is written into `dependencies.md` section 4.1.
## [2026-08-03] finding | The observability path and the PRD N1 measurement protocol are ONE mechanism rather than two, which nobody had noticed: N1 states its budgets in the terms a browser already reports (a main-thread task over the long-task line, an interaction past the perceived-lag threshold at the 75th percentile), so real-user telemetry is a stronger source for the 10.5 measurements than a bench, and the field tablet that is hardest to bench is the device that reports itself.
## [2026-08-03] decision | Grafana is recorded as the owner's stated preference for the backend, in `dependencies.md` section 4.1 rather than in the foundation, because a tool is an ADR and not a constitutional decision (the 9.6.5 precedent, where the component-catalog principle is the foundation's and Storybook is not). It composes with the neutrality property rather than fighting it, since Alloy speaks OTLP. Faro is the piece that actually answers "catch the first users' bugs", and it doubles as the real-device source for N1. Two shapes to weigh in the ADR: Grafana Cloud free (10k series, 50 GB logs, 50 GB traces, 14-day retention, three users) against self-hosted LGTM, whose own documentation calls the docker image development and demo rather than production.
## [2026-08-03] decision | **OQ-22, OQ-23 and OQ-24 opened and deliberately NOT scheduled**, under a standing rule written once: none is decided until a measured need appears, because each is an optimisation that adds complexity somebody maintains forever, and deciding one early is the error rather than the delay. What each carries now is the set of invariants that already narrow it. OQ-22 (edge caching, CDN, tile invalidation): I4 plus the access-denial-is-revocation rule forbid a shared edge cache in front of tenant geometry and leave a clear shape for one in front of public content, and the material in `data-and-tooling-references.md` section 1.5 is a decision SHAPE with no authority behind it. OQ-23 (rate limiting and quota): OQ-3's no-unlimited rule makes a quota the enforcement half of a promise already made, and a refusal must be typed and retryable rather than a dropped operation. OQ-24 (scale-out and the balancer): the application tier is already horizontally scalable by construction because section 10 put ordering in PostgreSQL, and ADR-0004 measured the ceiling and its shape.
## [2026-08-03] decision | The "pre-implementation" sentence was corrected in the foundation section 0 and in `CLAUDE.md`, in the same round rather than as a separate patch, since a round was open anyway. Both said no application code existed, which stopped being true on 2026-08-01 with the scaffold and again on 2026-08-03 with the containers and the CI gates.
## [2026-08-03] decision | **Foundation v0.17: OQ-4 closed.** The first vertical slice is the one the question already named as the candidate: an element created offline on one client, flushed on reconnect, appearing on another. Chosen because it is the smallest slice that exercises I1, I2, I3 and I9 together with the section 10 ordering authority, so every part of it becomes a permanent test rather than a demo. The boundary is written in both directions, and **the excluded half is the load-bearing one**: conflict resolution and preserve-not-discard are the NEXT slice, OQ-8 stays the engineer's, and presence, tiles, styling, analysis, the capability surface, extensions and the agent are all out. Said out loud in the canon so nobody misreads a green build: this slice proves I2 and I9 and does NOT prove C7 or I8, because a conflict rule with nothing to order is untestable.
## [2026-08-03] decision | Linear moved from a dedicated Mapsift workspace to a **dedicated team inside the owner's personal workspace**, because switching workspaces between personal projects was the real bottleneck. The isolation got stronger rather than weaker: an API key scoped to the Mapsift team is a wall, while remembering which workspace you are in is discipline. Verified after the switch that `list_teams` returns only the Mapsift team. Numbering restarted, so the first real issue is MAP-1 rather than the MAP-5 the handoff had predicted.
## [2026-08-03] decision | The first Linear project was created FROM foundation OQ-4 rather than the reverse, which is the git-owns-the-contract boundary applied: `First vertical slice: the sync spine`, six milestones, MAP-1 to MAP-24, eight team labels, every issue citing the requirement it traces to. Assessment recorded with it: **the scope is NOT closed enough for a complete backlog**, because OQ-8 and OQ-5 gate whole PRD families, three of the N1 budgets are unmeasured and each decides a design rather than a finish, and four ADRs are forced by the first code. A backlog beyond this slice would be issues tracing to open questions, which the project's own rule calls noise.
## [2026-08-03] trap | Two things cost time on the Linear switch and are now in the `linear-workflow` skill. The `Authorization` header must carry the **`Bearer ` scheme**; without it Linear answers 401 with `invalid_token`, which reads like a revoked key and is not. And **`claude mcp get <name>` prints the header value in clear**, leaking the key into whatever transcript is running, which is exactly what happened; `claude mcp list` shows health without the secret.
## [2026-08-03] finding | The two Linears on this machine are isolated because they live in **different files**, which was nowhere in writing: Mapsift is local scope in `~/.claude.json` keyed by the project directory, and the work Linear is project scope in the other repository's own `.mcp.json`. No user-scope server exists, which is the only scope that would apply everywhere at once. The skill carried the principle of isolation and never the runbook, which is how a rule gets executed wrong in a hurry.
## [2026-08-03] finding | Sweep for stale references to the old Linear workspace across `specs/` and `.claude/`: **none left**. The only hits are the two deliberate historical records of the move itself, and there is no `linear.app/mapsift` URL anywhere. The fan-out done when the workspace changed had already covered it, which is the fan-out rule working rather than luck.
## [2026-08-03] decision | Five orientation documents were created on the Linear team (Start here, what Mapsift is, how it works, where we came from and where we are going, how we work). **Each opens by declaring itself non-authoritative and pointing at git**, because a second copy of the canon outside the fan-out is exactly the drift the governance rule exists to prevent. They orient a reader and cite the canon; they never restate a decision as if they held it.
## [2026-08-03] finding | The Linear MCP can create team documents but **cannot set a team description, add resource links, create resource sections, or wire the GitHub integration**. Those four are UI actions, so the runbook for them lives with the owner rather than with the agent.
## [2026-08-03] decision | The Linear operating procedure was written into the `linear-workflow` skill, which is its documented home (the foundation section 14 and `CLAUDE.md` both delegate the tracker procedure there). It adds what one issue is, the status lifecycle with **red and green both inside In Progress** because the two-window protocol is one unit of work, a five-point definition of done, priority meanings, one-issue-in-progress-per-person with parallel work in worktrees, bugs needing a trace to the invariant they violate, and the project and milestone lifecycle. `dev-workflow` keeps everything from the branch onward and the two skills deliberately do not restate each other.
## [2026-08-03] decision | **XP is adapted rather than performed, and the three replacements are on the record so nobody re-adds the ceremony.** The planning game is replaced by the closed canon, since scope is decided in the foundation and the PRD before it reaches a tracker and there is no velocity to bargain with. Pair programming is replaced by the two-window protocol, which buys the same independent-check property without two people on one keyboard. The on-site customer is the embedded domain engineer, whose questions are marked open in the canon rather than guessed at in a planning session. **Story points, velocity, iteration commitments and burndown are dropped**, because all four forecast a scope still under negotiation and this one is closed. Cycles stay off until a named symptom appears rather than on a calendar.
## [2026-08-03] finding | Closing verification of the authority chain after three rounds in one day: foundation v0.17 and PRD v0.12 with every current-version pointer agreeing across `CLAUDE.md`, `index.md` and the PRD authority line, the OQ range at 1 to 24, the N range at 1 to 12, C1 to C14 unchanged, and both the v0.16 and v0.17 changelog entries present. One stale pointer was found and fixed (the PRD authority line still read v0.16); the two `dependencies.md` citations reading v0.16 are provenance and correctly stay, since a decision-provenance citation is history and is never bumped.
## [2026-08-03] decision | The Linear-to-GitHub integration is on, scoped to the `mapsift` repository alone, with the pull-request automation configured **against Linear's default**: a PR opened moves the issue to **In Review** and a merge moves it to **Done**, and nothing moves an issue INTO In Progress automatically. The default (opened moves to In Progress) is wrong here because In Progress means the two windows are working and a PR exists only after they finished, so the default would make the status lie about where the work is. **GitHub Issues Sync stays off**, and it is a separate feature from the PR linking: it is two-way and would put the contract and the execution state on both sides of a loop, which the git-owns-the-contract boundary forbids. The PR linking is unidirectional and is all this project wants.
## [2026-08-03] decision | **Issues, Projects and Wiki were turned off on the public GitHub repository**, each because it is a surface where a second copy is born outside the fan-out. Issues would be an untriaged second inbox that also collides with the rule that an issue exists only when it traces to the canon; Projects would be a second board, and status in two places diverges every time; the Wiki is the worst, because duplicated documentation fails silently rather than loudly and ages against the foundation with nobody noticing. **The revisit trigger for Issues is the first real users**, the same trigger the observability backend carries, since a support channel with nobody behind it is worse than no channel.
## [2026-08-04] decision | **ADR-0005 closes the tenant-isolation mechanism (MAP-1, agenda item 1): row-level security, enabled AND forced, with per-tenant views rejected.** Views lose on three counts: they need DDL for every new tenant, which turns creating a personal account into a schema migration and contradicts PRD M1; they protect reads and protect writes only with `WITH CHECK OPTION`; and the single parameterised view that avoids per-tenant DDL is row-level security with the guarantees removed, because any role with privileges on the underlying table reads straight past it. Four roles, none privileged (`mapsift_owner` migrations, `mapsift_app` runtime, `mapsift_tile` reader, `mapsift_tokens` key custodian with no data privilege), and **`BYPASSRLS` granted to nobody**.
## [2026-08-04] trap | **The empty-string trap, and it is the one nobody expects.** A transaction-scoped `set_config` does not revert to *unset* when the transaction ends; it reverts to the session value, which for a never-set custom parameter is the **empty string**, and `''::uuid` throws. So the obvious policy `tenant_id = current_setting('mapsift.tenant_id', true)::uuid` works on the first transaction of a connection and raises `invalid input syntax for type uuid: ""` on every one after it, which appears **only on a pooled or persistent connection** and reads like a broken database rather than a broken policy. The fix is `nullif(current_setting('mapsift.tenant_id', true), '')::uuid`. Measured against PostgreSQL 18.4 on 2026-08-04.
## [2026-08-04] finding | **The leak has a shape and it is the session-scoped binding.** Measured: `set_config(..., false)` survives into a later transaction on the same connection, so with `CONN_MAX_AGE` or the psycopg pool it reaches the **next request**, which may belong to another tenant. psycopg hands a connection to the next client in idle state without clearing session state (cleanup is the application's `reset` callback), and Django's own manual says the same in reverse: restore the parameter, force it per request, or stop reusing connections. Hence the rule: the binding is **transaction-scoped, always**, and a session-scoped one is prohibited by name.
## [2026-08-04] finding | **The tenant parameter is a binding, not a capability, and the limit is irreducible.** Measured: an unprivileged role sets the parameter to any value it likes, `REVOKE SET ON PARAMETER` changes nothing, and `pg_parameter_acl` holds no entry, because a custom two-part parameter is a placeholder the server accepts from anyone. So row-level security stops a **query** from crossing the wall and does not stop **code that can run arbitrary SQL on an already-bound connection**. That is the honest edge of what the wall buys, and it is why the parameterised binding statement is a security control here rather than a style preference.
## [2026-08-04] trap | **Referential integrity is a documented hole in the wall, and it was measured open.** PostgreSQL states that referential integrity checks always bypass row security. In practice: a row bound to tenant A **successfully created a foreign key to a row of tenant B**, and a primary-key collision against an invisible row of another tenant reported itself as a unique violation. A **composite key over `(tenant_id, id)` closes the first completely** (re-measured: cross-tenant reference now fails, same-tenant succeeds), at one unique index per table, and every **natural** unique key is unique per tenant rather than globally.
## [2026-08-04] finding | **FORCE is what makes the wall true in the test suite, not only in production.** Measured: the table owner with row-level security enabled and not forced sees every tenant (3 of 3), and so does a `SECURITY DEFINER` function owned by it; `FORCE ROW LEVEL SECURITY` closes both at once (2 and 2). Migrations and the Django test database are created by the owning role, so **without FORCE the entire suite would run outside the wall it exists to prove**. A `BYPASSRLS` role still sees everything after FORCE, which is why no role in this system is granted it.
## [2026-08-04] decision | **The tile path's contract is fixed even though the tile server is still its own ADR.** Any candidate must connect as the unprivileged reader role, must receive the tenant **inside the transaction serving the tile from a claim the database itself verifies** rather than from a raw identifier in a URL, and must **raise rather than return an empty tile** when the claim is absent, expired or unverifiable, because an empty tile is a wrong answer and a wrong answer presented as data is the silent-discard sin in another costume. Verified against Martin's documentation on 2026-08-04: a function source receives `z`, `x`, `y` and `query_params json` **and nothing else**, no headers and no identity, so the claim travels as a short-lived signed capability the API tier mints and a `SECURITY DEFINER` verifier owned by the key custodian checks in-database. A pool per tenant was rejected (connections would grow with a tenant population M1 expects to be mostly personal accounts) and so was an authenticating proxy (a session variable cannot be carried over HTTP into somebody else's pooled connection).
## [2026-08-04] decision | **PRD v0.13, the fan-out of ADR-0005 and nothing else.** T6.1's Open/ADR is closed by that ADR, and N2's acceptance grows the cases its probes wrote: no application role holds `BYPASSRLS` and none owns a tenant-owned table, asserted from the catalogue rather than reviewed; an unbound query returns nothing **and** raises in the application, because the policy's silence is indistinguishable from an empty tenant; and a write bound to one tenant cannot reach another's row through a foreign key or discover it through a unique-key collision. The last one is the case no reading produced and only the probe did.
## [2026-08-04] decision | **ADR-0006 closes the identifier variant (MAP-2, agenda item 2): a random 128-bit identifier, not a time-ordered one.** Four reasons in weight order: the locality a time-ordered identifier buys is contingent on a clock this model distrusts by construction (I10); M3 requires opacity and a time-ordered identifier publishes its creation instant; N9 puts the operation identifier in every log line on the sync path, so that instant would travel everywhere, and N5 separates a parcel vertex (the subject matter) from a person's sequence of capture instants (their whereabouts); and ADR-0004 already owns ordering through the per-project and per-feature versions, so the identifier does not need a second job.
## [2026-08-04] finding | **The measurement inverted the expected trade, which is why it was run.** PostgreSQL 18.4, 500,000 rows per arm inserted as 500 batches of 1,000: perfectly ordered gave a 15 MB index at 89.98% leaf density; random gave 19 MB at 71.87%; and **time-ordered with one row in five minted on a clock up to thirty days out gave 37 MB at 52.86% density**, which is 2.45 times the well-behaved ordered index and roughly twice the random one. Skewed keys do the worst of both, splitting pages in a band that is also being appended to and leaving the halves behind. So the choice was never ordered against random, it was ordered-if-every-clock-is-right against random-always.
## [2026-08-04] finding | **Two honesty notes recorded with those numbers, because a measurement without its conditions is decoration.** The whole dataset fit inside the container's 128 MB `shared_buffers`, which is the regime where random keys are **cheapest**, so the insert-time column understates the random penalty at a scale where the index exceeds memory; the size and density columns are structural and do not. And the skew arm is a model rather than a census: one row in five and thirty days are a plausible field population, so the direction of the result is robust and the exact multiple is not.
## [2026-08-04] trap | **A time-ordered identifier is not opaque, and PostgreSQL 18 hands you the proof.** `uuid_extract_timestamp()` returns the creation instant of a v7 identifier in one call and returns nothing for a v4. Adopting v7 would mean writing a rule that the embedded time is never read as evidence and then defending it forever against a field that visibly contains the answer. A property enforced by the shape of the data beats a property enforced by a paragraph.
## [2026-08-04] decision | **PRD v0.14, the fan-out of ADR-0006 and nothing else:** M3's Open/ADR closed, the trade it described recorded as not existing in this product's conditions, and created-at plus applied-at remaining the only times in the trail now because the identifier carries no third one rather than because a rule says not to read it.
## [2026-08-04] decision | **The spec-per-task the authority chain always ended at now has a home: `specs/tasks/`, one file per Linear issue that needs one, named by its issue ID.** It **assembles and cites** what the foundation, the PRD and the ADRs already decided into the one contract two windows read, and it decides nothing of its own; where it disagrees with a document it cites, the cited document wins. Not every issue earns one: the trigger is requirements an implementer must hold at once being spread across several documents, which `MAP-3` is (M1, M3, ADR-0005, ADR-0006, N2, C3, C4).
## [2026-08-04] decision | **First advisory assessed and deliberately not acted on, with the reasoning in `dependencies.md` section 5.9 rather than only in a dismissal box.** The `@hono/node-server` path traversal is real and does not reach this product on three independent grounds: it is **Windows-only** by its own text while this runs on Linux containers, the package is a **development dependency** reached through `@angular/cli` and `angular-eslint` via the MCP SDK so it never enters a bundle or a server, and nothing here serves static files through Hono at all. **The offered fixes are worse than the exposure:** Dependabot's path downgrades `@angular/cli` to 21.0.4, which ADR-0002 forbids in spirit since generation is CLI-first so an artifact matches the installed schematic, and the `overrides` alternative forces a major under a dependency declaring `^1.19.9`. Re-check trigger written into the entry: an Angular CLI release whose MCP SDK takes the patched Hono.
## [2026-08-04] finding | **One gap that MAP-5 inherits, found by reading the MAP-3 code: `Project` carries no `UNIQUE (tenant_id, id)` while `Workspace` does**, so a composite reference from `layer` to `project` has nothing to point at until MAP-5 adds it, by the same ADR-0005 section 5 rule that put the matching constraint on `Workspace`. **Corrected in the same session:** this line originally paired it with `django.contrib.gis` being absent from `INSTALLED_APPS`, reported as a second finding. That is **not** a finding, it is a **decision already recorded and verified** in `dependencies.md` section 1, with its reason (the app loads GDAL and GEOS at import while the host is not required to carry them) and its own named trigger, "it goes in with the first geometry model, inside the container", which MAP-5 is. The lesson is the cheap one: check whether the canon already carries a thing before reporting it as discovered.
## [2026-08-04] decision | **`specs/tasks/MAP-5-layers-and-features.md` written, and the load-bearing part of it is a scoping call read off foundation OQ-4 rather than invented.** M2's full shape carries an attribute schema, a style, provenance and a legal-weight marker; the slice's operation catalog is create a feature and set its geometry, so each of those is deferred **with the owner that holds it** (M6, F1 to F4, B4, and M7 which waits on OQ-8) instead of modelled blind. Two consequences a later window should not re-derive: the source-CRS provenance of M5's Shape is an **import-time** fact and import is not in this slice, so the enforceable half of M5 rule 1 today is only that the geometry column declares EPSG:4674; and M2's acceptance is written in terms of an operation queue MAP-7 to MAP-14 have not built, so what MAP-5 actually owns is the **pure decision underneath it**, which is that the path a feature takes is a function of its layer's storage class alone.
## [2026-08-04] decision | **ADR-0007 fixes the `apps/api` layout, and the trigger for it was written in ADR-0002 section 5 long before it fired** ("the Python rules stay candidates for their own ADR when they stop restating the canon and start deciding"). One application package `mapsift/`, one subpackage per **domain** and never per layer, `config/` never holding domain code, and per-package file roles: `rules.py` pure, `selectors.py` reads, `services.py` writes and is the only writer, `capabilities.py` the published surface, one thin django-ninja `api.py`. Surveyed rather than assumed: cookiecutter-django 2026.31.2 for the two-tier shape, the HackSoft styleguide for the services-and-selectors split and its list of where logic may NOT live, django-ninja's own guide for `api.py` per app (the DRF-shaped `api/` subpackage does not transfer), and import-linter 2.13 for enforcement.
## [2026-08-04] finding | **What forced the layout ADR before MAP-5 rather than after it was a line of code, not a preference.** `accounts/binding.py` holds `tenant_scope` and `TenantOwnedManager`, which are the ADR-0005 wall's mechanics and are needed by every tenant-owned table. MAP-5 creates the second app, and under the old layout that app would have written `from accounts.binding import ...`, making a platform primitive something imported out of a domain package. One app, one migration and five test modules is the cheapest this move will ever be.
## [2026-08-04] decision | **The dependency direction is a CI gate rather than a review item, and `import-linter`'s `protected` contract is why the current version is taken.** A `layers` contract with two tiers asserts little today and is wired anyway, on the ADR-0001 section 6 precedent that a gate exists before the code it governs. The half that is not thin is `protected`, which restricts who may import a module **directly** from an allow-list, turning "reach a package through its selectors and services, never its models" from a convention a reviewer must remember into a failing build. Recorded with the syntax trap: in a `layers` contract `|` makes siblings independent and `:` lets them import each other.
## [2026-08-04] decision | **The capability layer got a home before the first capability exists, which is the only order in which it is cheap.** Foundation 9.5 makes every data operation a named, serializable, machine-described, composable capability that the app, the extensions, the SDK and the agent all consume; that is **not** a `services.py`, and a layout with nowhere for the registry is wrong for this product whatever else it gets right. Capabilities are declared in the owning package's `capabilities.py` and the registry **collects by walking packages**, on the same reasoning as the N2 catalogue test: a central list somebody maintains goes stale silently, and a missing capability is invisible to the agent rather than loudly broken.
## [2026-08-04] trap | **A large module move poisons the mypy cache, and the symptom lies.** After `accounts/` became `mapsift/accounts/` and `binding.py` moved to `mapsift/common/`, `mypy --strict` died with `INTERNAL ERROR` pointing inside `django-stubs/http/request.pyi`, which reads like a broken type checker or a bad stub release and is neither: `.mypy_cache` still held entries keyed to the old module paths. `rm -rf .mypy_cache` and it passes on all 24 files. Same family as the django-stubs environment trap already recorded, where the real cause is nowhere near the file the error names.
## [2026-08-04] decision | **ADR-0007 executed (MAP-28), and `just check` is the proof rather than the claim.** `accounts/` moved to `mapsift/accounts/`, `binding.py` to `mapsift/common/binding.py`, `tenancy.py` became `accounts/rules.py` (the file role it always was), and the per-package tests moved under their package. **The empirical half:** the gate is green with the migration applying unchanged, which is what proves the recorded app label did not move, since `AppConfig.label` is the last component of the Python path and `apps.get_app_config("accounts")` is asserted by a test that was not edited. `lint-imports` reports both contracts KEPT.
## [2026-08-04] finding | **A sentence of ADR-0007 could not be executed and was corrected inside it rather than worked around**, in the ADR-0005 section 7 form. It put shared fixtures in `apps/api/tests/`, and pytest resolves a `conftest.py` by directory, so a fixture declared there is invisible to a test under `mapsift/accounts/tests/`. Not hypothetical: `alice` and `bob` serve both the account-tree tests, which belong to a package, and the wall's suite, which does not. Shared fixtures live in the stack's root `conftest.py`; `apps/api/tests/` holds cross-package tests.
## [2026-08-04] trap | **`choices=` cannot take a framework-free enumeration in Django 5.2, and the error names the wrong thing.** ADR-0007 keeps `rules.py` pure, so the storage class and geometry kind are plain `StrEnum` rather than `TextChoices`, and there is no `.choices` to read off. Handing the enum class itself to `choices=` raises `TypeError: EnumType.__call__() missing argument: 'value'`, from `normalize_choices` catching it in its callable branch after `str`-valued members already failed the iterable guard above. The fix is a two-line helper producing the mapping; the cost is the price of the enums not importing Django, which is the point.
## [2026-08-04] finding | **A pure closed-set decision is enforced by the type checker before anybody runs a test, and it was probed rather than assumed.** `enters_the_operation_queue` matches every `StorageClass` member and ends in `typing.assert_never`, so adding a member fails `mypy --strict` with `Argument 1 to "assert_never" has incompatible type "Literal[StorageClass.UNDECIDED]"; expected "Never"` before the suite is reached. Also probed: `warn_unreachable = true` does **not** flag the `assert_never` call, which mypy special-cases, so the two settings compose rather than fight.
## [2026-08-04] finding | **An index the migration list asked for was refused with an argument, and the refusal was right.** ADR-0005 section 5 fixes that a tenant-scoped index **leads** with the tenant and does not enumerate which indexes exist; `specs/tasks/MAP-5-layers-and-features.md` had enumerated one that the composite unique key `(tenant_id, project_id, id)` already answers from its leading columns, making it a strict prefix duplicate that serves no read and costs every write. Corrected in that spec with the reasoning. The general lesson is the useful half: a spec-per-task that enumerates artifacts rather than citing the rule will eventually enumerate one the rule does not want.
## [2026-08-04] trap | **A test that proves a field does not exist is a static error before it is the runtime one being asserted.** `Feature(storage_class=...)` under `pytest.raises(TypeError)` is exactly the M2 requirement that the storage class is the layer's, and django-stubs builds a model's `__init__` from its fields, so `mypy --strict` refuses the call with `Unexpected attribute "storage_class" [misc]`. No production code fixes it, because the only thing that would is the field the test exists to forbid. The fix is **one `# type: ignore[misc]` on that line**, carrying the note that removing it removes the test's subject; a module-scoped `disable_error_code` was tried first and is wider than the problem, since `misc` is a busy code and the module is three hundred lines.
## [2026-08-04] finding | **`accounts` has no service that creates a workspace or a project, and two places now build those rows by hand.** `conftest.py`'s `_party` has done it since MAP-3 and `second_project_of` joined it in MAP-5, both calling `.objects.create` directly because there is no sanctioned route to call. It blocks nothing today and it is the reason a cross-package test reached for a model in the first place. When `accounts` grows `services.create_workspace` and `services.create_project`, both helpers move onto them and the shared fixtures stop constructing rows by hand.
## [2026-08-04] decision | **How a window is briefed is now written in `specs/testing.md` section 1, because it was a rule that existed only in whoever was orchestrating.** A briefing carries the goal, the boundary, where the authority lives and the standard, and it carries **no step list, no file list and no name derivable from the canon**. Two reasons, and the second is this repository's own: a window handed steps transcribes rather than decides, so the review can only catch transcription and an error in the briefing rides through unchallenged, which destroys the independent check the two windows exist to buy; and a briefing restating a requirement is a second copy outside the fan-out, in a message that is neither versioned nor reviewed. **The one thing handed over is evidence rather than instruction:** what cost a measurement or a probe goes to the window with its date, because it is not the window's decision to make and would otherwise be bought twice.
## [2026-08-04] trap | **The storage frame was a default that the write path converted into, not a guarantee, and only one of its two writers was ever closed.** Measured on this branch against PostgreSQL 18.4 and PostGIS 3.6: a raw insert of a parcel in **EPSG:31983**, which is how Brazilian cadastral data arrives, is refused by the column's own **type modifier** with SQLSTATE 22023, `Geometry SRID (31983) does not match column SRID (4674)`, so the writer that never passes through the ORM was closed and needed no constraint; the **same parcel through the ORM was accepted, converted to degrees by an `ST_Transform` GeoDjango wraps the value in when the SRID differs, and its source frame discarded with nothing raised**. That breaks M5 rule 1, which records the source CRS and never discards it, and rule 3, which makes a datum transformation an explicit recorded decision. The lesson generalises past geometry: **a column's declaration is a shape and never a behaviour, and a test that reads the declaration proves neither of the paths that write to it.**
## [2026-08-04] decision | **The storage frame is a field's guarantee rather than a column's declaration, and it lives in tier 0 because every geometry column the product ever adds owes the same thing.** `mapsift/common/geometry.StorageFrameGeometryField` refuses a geometry declaring another frame at the save path instead of converting it, so what is stored is coordinate for coordinate what was sent (M5 rules 1 and 3). What stays open is assigned rather than left: a geometry arriving with **no** frame declared is still taken as already in the storage frame, and the boundary that must turn that assumption into a recorded fact is the flush envelope's (M8, M11, M12); the **legitimate** conversion, which records the source CRS and transforms once authoritatively, is import's (B2).
## [2026-08-04] finding | **A spatial index's deferral got the trigger the canon requires, and the reason first recorded beside it was wrong in the clause that mattered.** The comment said a tenant-leading composite would serve "a query nobody has written"; that query is written, in ADR-0005 section 6 and foundation section 6, because tiles are served straight out of PostGIS with the isolation policy adding the tenant predicate to every query. **Measured on PostgreSQL 18.4, PostGIS 3.6, 1.000.000 features across 100 tenants, 2026-08-04:** with tenants **sharing an extent** the composite answers in **0,12 ms against 2,14 ms** and scans **18 index entries against 12.492**; with tenants **geographically separate** it loses, **4,02 ms against 2,40 ms**; and it costs **86 MB against 40 MB**. So the plain GiST is right while tenants are separated on the map and inverts by an order of magnitude the day two consultancies work the same municipality. The trigger is written in `specs/tasks/MAP-5-layers-and-features.md` section 3, in the shape foundation section 6 uses for the tiling gate, and it is deliberately two conditions rather than one.
## [2026-08-04] decision | **The feature table deliberately carries no `UNIQUE (tenant_id, id)`, and the argument is the direction of derivation rather than the cost of an index.** M15 makes the operation log the source and the current state its **projection**, so a referential constraint from the log to the feature would make the source depend on its own derivative, and the log has to outlive the feature it describes (C7, M15, with physical deletion itself open under OQ-20). That is why M9 carries the target path as an **address in data** rather than as a foreign key. Trigger: the first tenant-owned table that must reference a feature by a real referential constraint. The preventive counter-argument is answered rather than waved away: adding it later is `CREATE UNIQUE INDEX CONCURRENTLY` then `ADD CONSTRAINT ... USING INDEX`, which builds without holding writes out, so this is not the live-table migration that justified giving `Project` its key in the same change.
## [2026-08-04] trap | **`import-linter`'s `protected` contract applies every allowed importer to every protected module, so the obvious way to cover a second package grants each one access to the other's models.** Probed rather than read, with a throwaway module in `layers` importing `accounts.models`: two contracts of one package each report it **BROKEN**, and folding them into one contract naming both packages reports **KEPT** with the same import still there. It also has **no `exhaustive` option**, unlike the `layers` contract corrected in ADR-0007 section 4, so its module list is the one part of the gate a new package is silently absent from, and MAP-5's own package was absent from it. Closed by `apps/api/tests/test_dependency_direction.py`, which enumerates the packages holding models from disk and asserts the gate's allow-pairs are exactly one per package, the way the N2 suite enumerates tenant-owned tables from the catalogue.
## [2026-08-04] finding | **A rule invented in a spec-per-task contradicted a requirement nobody thought to check it against, and only half of that is an implementing window's to fix.** `geometry_is_admissible` compared a layer's declared geometry kind against a feature's by identity, which **D3** forbids: multipart geometry and a ring with an enclave are a domain requirement, and a legal reserve is frequently one or the other, so identity would refuse one. The declared kind is a **family** and that half is closed. The other half is that the rule **traces to no requirement at all**: M2 gives a layer a geometry kind and never says a disagreeing feature is refused, so under `specs/testing.md` section 6 its test is a candidate for deletion, and raising it into the PRD is the owner's. Both roads with their costs are in `specs/tasks/MAP-5-layers-and-features.md` section 2.1.
## [2026-08-04] finding | **An omission in a spec's deferral list is worse than a deferral, because the list exists so that every omission names its owner.** M2's Shape gives a layer a `kind` of vector or raster; MAP-5 neither carried it nor deferred it, so with `geometry_kind` NOT NULL a **raster layer is unrepresentable** while the served storage class exists partly for it. Deferred now with its owner named, the served path whose tile server is still its own ADR, and with the reason it is cheap rather than a debt: both halves are additive and backfillable, since every layer that can exist before a raster path exists is a vector layer.
## [2026-08-05] decision | **PRD v0.15: the layer's declared geometry kind stops being a label and becomes a contract on its features (M2), and the refusal is specified where it happens (M9).** The rule existed first in a spec-per-task that had **invented** it, which under `testing.md` section 6 made its test a test with no requirement; an adversarial review found that and the round is the correction rather than a new idea. **What decided it was not the aesthetic argument but three things the next slice builds, none of which can defend itself:** a paint specification is per geometry type, so a stray geometry does not render and does not say so, which is AR1 through an unlocked door; D8 makes area and perimeter automatic and authoritative on a polygon layer while a line inside one has no area, in a product computing a legal reserve's hectares; and an interchange format carries a single shape type in its header, so a mixed layer fails to export or exports incomplete against the interoperability J1 promises. The cost is small because **the caller arrives anyway**, M9 already requiring an operation outside the catalog to be rejected with a typed error.
## [2026-08-05] decision | **The refusal is flag-and-retain and never a discard, and that half is what decides whether the rule protects field work or destroys it.** The geometry is drawn offline in the field and the refusal happens at the flush, so a raise that drops the operation is preserve-not-discard broken by another name (C7, C13). It takes the shape T5.2 already fixes for the operation whose author lost authorization: typed error, flagged, retained for inspection.
## [2026-08-05] finding | **The family rule cannot descend to the database, and the trigger that could is declined with its reason rather than left unmentioned.** PostgreSQL constrains a geometry column by its type modifier, which is per **column**, while the permitted family is a value on another **row** in another table, so the I4 answer (a composite key) has no equivalent here. A trigger joining to the layer would close the non-ORM path; it is declined because it adds a lookup per insert on the highest-volume table on the flush path, puts server-side logic outside the ORM with no precedent in this stack, and buys complexity without the measurement the performance rule requires. The rule therefore lives in `rules.py` with its caller on the write path and a non-ORM writer can violate it, which is the honest difference from the storage frame that the column closes on its own.
## [2026-08-05] decision | **The layer's `kind`, vector or raster, stays deferred, and the argument that settles it is one nobody had written.** Carrying it now forces `geometry_kind` to become nullable now, relaxing an invariant that currently bites in order to accommodate a raster layer no code path in this slice can create, since raster reaches the product only through import. The reversibility half was checked rather than assumed: a column with a default and a `DROP NOT NULL` are catalogue changes rather than table rewrites, which is what makes this case unlike the project-consistency one that was deliberately closed early because a **constraint** on a live table requires a scan.
## [2026-08-05] finding | **A fan-out reaches the documents and stops before the code, and the sentences it leaves behind are the dangerous kind.** When an owner decision lands, every sentence a previous window wrote saying what the canon does **not** say becomes false, and those sentences live in docstrings and in spec instruction lists rather than in the requirement the fan-out targets. The v0.15 round reached the PRD, `index.md` and `CLAUDE.md` and stopped there, leaving three: a test module's header, the same paragraph duplicated in production code beside it, and an item in the spec-per-task's own instruction list which was worse than both because it sat in a list a window reads to write tests, with no closure next to it and a pointer to the section that now contradicted it. **The rule this adds to the fan-out:** after closing a decision, grep for the phrases that asserted the absence of it, because a requirement gaining an acceptance turns "the canon does not require this" into a lie everywhere it was written.
## [2026-08-05] decision | **Comment hygiene pass over `apps/api`, and the property that makes it checkable is not the ratio.** Measured with one rule stated before it was used (a marked line is a non-empty line that is a comment token or lies inside a module, class or function docstring; the denominator is non-empty lines): production 161/527 = 31% to 127/493 = 26%, tests 205/928 = 22% to 165/888 = 19%, generated 51/327 = 16% untouched, overall 417/1782 = 23% to 343/1708 = 20%. Seventy-four prose lines removed. **Zero executable lines changed, proven rather than promised:** the executable AST of all ten changed files is identical before and after with docstrings stripped, verified independently, and no test lost a requirement identifier across the thirty examined. Three patterns accounted for the cut: the same fact stated at module level and again at function level, reasoning already living in this file or in a dated spec correction restated in a docstring (a third copy outside the fan-out), and test docstrings carrying a paragraph where `testing.md` section 6 asks for an identifier and the behaviour asserted.
## [2026-08-05] finding | **The 13% of 2026-08-01 is not a number `apps/api` can be held to, and chasing it there would cut traps.** That measurement was hygiene applied across everything written in one session over four ecosystems, and any aggregate is dominated by `libs/ui`, which is 326 marked lines over 13,667 and is a vendored library of primitives. `apps/api` is almost entirely pure decisions and invariant acceptance tests, which is precisely the code the rules **require** to carry traps and requirement identifiers, so its structural floor is higher. The comparable figure is the four-ecosystem aggregate; within this directory the per-file table says more than the bucket mean, because a percentage over a forty-line module of pure decision is not the same measurement as one over a three-hundred-line test module.
## [2026-08-05] finding | **A measurement's numerator is part of the measurement, and the orchestrator's was wrong.** The first pass counted a docstring as its line count plus its delimiters, inflating every docstring by about one line and putting production at 42% where the accurate rule puts it at 31%. The direction of the finding survived either rule and the target did not move, but the recorded number is the accurate one. This is the same discipline PRD N1 imposes on a performance budget applied to a hygiene metric: a number without the rule that produced it cannot be re-measured or falsified.
## [2026-08-05] decision | **ADR-0008 ratifies the development workflow and tracking as one decision**: git owns the contract and Linear owns execution state bridged by the task identifier, the two-window protocol under an orchestrator with sequential dispatch, the issue rules, the personal-workspace team structure, the one-pull-request rule for crossing changes, the refusal of autonomous issue-to-pull-request automation, and the rule that a skill injects the spec it depends on with a `!` command at dispatch, never a paraphrase and never a bare citation. It fills the hole the method port exposed: the `linear-workflow` skill was built to inject a workflow ADR's Decision and Mapsift's ADR-0004 is the sync ordering, so the injection pointed at a file that did not exist and came back empty, the silent-empty failure ADR-0005 section 4 names.
## [2026-08-05] finding | **The method port carried the sibling project's state as this repository's, and a name grep could not have caught it.** Eleven skills asserted a one-repository migration that never happened here (with two of them disagreeing about whether it had run), a `specs/constraints.md` that does not exist, invariants above I11, Angular 21 against a lockfile pinning 22, DRF and pnpm and Procrastinate for a django-ninja, npm and Celery stack, and two dead `sed` injections. All corrected against disk in the same pass that created ADR-0008. **The rule this adds to a port's fan-out:** sweep for state claims (repository layout, stack names, invariant ranges, version numbers, file paths), not only for the donor's name, because a false state claim executes while a wrong name merely offends.

## [2026-08-05] finding | **`@angular/aria` exists, is stable at 22.1.1, and lands on two requirements this project already carries.** First-party, headless directives for the WAI-ARIA patterns (listbox, combobox, menu, toolbar, accordion, tabs, tree, grid and the rest), carrying keyboard, focus, ARIA attributes and screen-reader semantics and **shipping no styles at all**. That shape is the best possible fit against U4, which says a vendored component is adopted only if it maps onto the Mapsift tokens: a headless library has nothing to map. It touches N7 (WCAG 2.2 AA on the chrome) and U10 (no bespoke re-implementation of a library primitive). Recorded in `dependencies.md` section 3 as a **candidate that is not adopted**, because `libs/ui` already carries about forty-five primitives with their own accessibility, and choosing between rebasing them, using it for new primitives only, or leaving it out is a decision about the component library rather than a fact about a dependency. Folded into agenda item 11, the component-catalog ADR.

## [2026-08-05] finding | **A secondary source was wrong about a framework's flagship feature, and only the primary source caught it.** The first search on the Angular v22 line reported "selectorless components" as its headline, and the finding was one edit away from being written into the canon as a gap in the Angular skills. Checked against the official documentation, selectorless has **not** reached developer preview and remains an RFC, while what actually went stable in v22 is Signal Forms, asynchronous signals and `@angular/aria`. This is the external-dependency rule producing its own justification: the aggregator was plausible, current-sounding and wrong, and the cost of believing it would have been a correction shipped against a feature that does not exist.

## [2026-08-05] finding | **MAP-6 ran both windows without a spec-per-task, and the gap is recorded rather than backfilled.** The task straddled the same-day adoption of ADR-0008: its red tests were dispatched before `specs/tasks/README.md` and the task-spec rule existed. The owner's verdict is this entry rather than a retroactive `specs/tasks/MAP-6-*.md`, because a task spec written after both windows have run is an artifact that guided nobody and would read as if it had. The next task picked up is the first to run the whole loop under ADR-0008.

## [2026-08-05] decision | **The MAP-6 review closed with two findings, and both closed by owner verdict.** The probe router's move from `config/api.py` into the new `config/probes.py` is ratified: a probe is operational surface rather than domain code, so the home is inside ADR-0007 section 2, and the behaviour is pinned by a test module that went unedited. The compose healthcheck comment is cut to its traps, one line for /ready-never-/health and two for Python-rather-than-curl, because the removed lines restated N12 and the pg_isready story this log already carries, which is the second copy the comment discipline exists to prevent.

## [2026-08-05] finding | **Migrated from the session handoff at its size budget: the three SP-1 method lessons.** The negative control is what earned the harness its credibility (the naive design lost 53.6 percent of committed rows at ten writers with the cursor finishing convinced it had seen everything, and only because the harness caught that did any later green mean anything). Identical failures across independent implementations are a harness bug and never a finding (all three candidates reported the same duplicate counts, which was a table not being reset between rounds). And the harness deadlocked itself with a connection left idle in transaction blocking the next test's DDL, the same family as the flaw that disqualified the rejected candidate. The numbers live in ADR-0004, the stages and exit criteria in `specs/spikes/SP-1-postgres-ordered-sync.md`, and the obligation that survives is MAP-24, the Stage B cases as real tests in `apps/api`.

## [2026-08-05] finding | **Migrated from the session handoff at its size budget: the first comment-hygiene measurement.** Applied on 2026-08-01 to everything written that session across the four ecosystems, comment and doc density fell from 39 percent to 13 percent, 236 lines removed against 72 added. The rule itself is law in `CLAUDE.md` and the owner's global directive; the 13 percent is a four-ecosystem aggregate dominated by `libs/ui`, and the 2026-08-05 hygiene entries above record why `apps/api` alone sits structurally higher.

## [2026-08-05] decision | **The ADR convention is reversed: the set reads as the current truth, edited in place with a dated note of what changed and why, and a new number is for a new decision area only.** Owner decision in the MAP-27 round. The reason supersession lost: a reader who opens a superseded ADR first and not its successor walks away with a wrong architecture, so the chain served the writer where the set must serve the reader, and git history carries the chain either way. Registered in ADR-0001's conventions with its dated note; the fan-out reached ADR-0002 (its three restatements), `index.md`, the toolkit README and the fan-out skill's target table.
## [2026-08-05] decision | **The wall answers the login question: a second permissive policy on `membership`, `FOR SELECT` only, keyed on the authenticated user (ADR-0005 section 8, MAP-27).** Permissive policies OR-combine, so the tenant policy is untouched and the addition is exactly the session user's own rows; `FOR SELECT` is load-bearing, because the `FOR ALL` variant carries a `WITH CHECK` that would let any user insert themselves into any tenant; the second parameter `mapsift.user_id` obeys decision 3's binding rules unchanged and is bound once per authenticated request beside the tenant (owner decision); decision 4's guard extends to it, so a membership read with neither binding raises rather than returning empty; and no HTTP surface ships with it, the trigger being the authentication surface itself (owner decision).
## [2026-08-05] finding | **I4's letter and M1's own requirement carried a latent tension from v0.11, and the login question is where it surfaced.** I4 said cross-tenant read or write is impossible while M1 requires a user with two tenants to hold one identity and two memberships the login path must enumerate before any tenant is bound. Resolved in intent's favour with the owner's approval: foundation **v0.17.1** names the exception in I4 (the reader's own membership rows and nothing else), PRD **v0.16** carries it in T6.1 and M1, and `CLAUDE.md` C4 points at ADR-0005 section 8. The owner attached a standing rule: a temporary exception carries its exit condition and leaves when the condition appears, tracked through the flow rather than through memory, which is why the ADR-0005 section 8 surface deferral now has a Backlog issue as its exit while the I4 exception itself is permanent by nature.
## [2026-08-05] decision | **ADR-0005 section 8 extended closing the MAP-27 implementation review: the both-bound answer is now letter, and it is witnessed rather than trusted to a comment.** The Spec axis traced the state with both bindings in force and found the correct answer (the reader's own rows alone, against the OR of two permissive policies) hanging on one ORM filter whose deletion left all 100 tests green while the login read inside a tenant-bound transaction would return that tenant's whole membership roll as the reader's own. The letter now names the answer and requires its witness, plus the two arms of the user binding's discipline (same-user no-op, second-user refusal), which the tenant scope had witnessed and the user scope did not.
## [2026-08-05] finding | **The session-user manager exposes the write API, and a bulk UPDATE or DELETE through it with only a user bound reaches zero rows in silence.** Found by the Canon axis of the MAP-27 implementation review and judged outside section 8's letter: writes stay under the tenant policy at the wall, ADR-0007 makes `services.py` the only writer, and no decided path writes through `Membership.of_the_session_user`. It is the one spot where decision 4's fail-loudly principle has a gap a misuse could walk through. Hardening deferred with its trigger named: the day membership gains any writer beyond `services.py`, or a bulk write path touches the model, this manager either refuses writes or grows the guard.
## [2026-08-05] decision | **MAP-27 closed milestone 1 through pull request #16, and it was the first task to run the whole ADR-0008 loop end to end.** Task spec at pickup, boundary decisions closed and registered before dispatch, sequential windows, review by running with three isolated axes each round, two correction rounds (the catalogue gate rebuilt from a grep into a shape comparison over `pg_policy`; the witness round with mutation evidence that the load-bearing filter bites), a hygiene round under green, four atomic commits with red before green surviving the rebase, CI green on the three jobs, and the automation moving the issue Backlog to In Progress to In Review to Done with nobody touching it.
## [2026-08-05] finding | **Migrated from the session handoff at its size budget: the operational-layer round's three rediscoverables (2026-08-03).** The OpenTelemetry Python logs SDK was still in development in May 2026, which decides the mechanism as standard library with trace identifiers injected and is law in `CLAUDE.md`'s observability bullet; the observability path and the PRD N1 measurement protocol are one mechanism, so real-user telemetry outranks a bench for the 10.5 measurements; and Grafana is the owner's stated preference, in `dependencies.md` section 4.1, composing with vendor neutrality because Alloy speaks OTLP.
## [2026-08-05] finding | **Migrated from the session handoff at its size budget: the full-canon re-read of 2026-08-03.** Foundation, PRD, ADRs, `CLAUDE.md`, market research and the derived indexes read end to end against disk: internally consistent, no derived document deciding what the authority left open. The two stale pre-implementation status sentences it found were corrected in the v0.16 round with the owner's authorisation rather than on a window's initiative, which is the precedent for who edits the constitution.
## [2026-08-05] finding | **Migrated from the session handoff at its size budget: where the Linear structure and its traps live now.** The 2026-08-03 workspace move (personal workspace, team Mapsift, the team-scoped API key as the wall, verified by `list_teams`), the Bearer-scheme 401 trap and the `claude mcp get` key-in-clear trap are carried by ADR-0008 section 3 and the `linear-workflow` skill; the handoff no longer duplicates them.
## [2026-08-05] finding | **Migrated from the session handoff at its size budget: the `@mapsift/ui` packaging correction of 2026-07-30.** The tree was flattened to `libs/ui`, the Nx files removed under the no-monorepo-tool rule, the package renamed with a peer dependency on Angular 22, and the source audited as a legitimate library: 45 shadcn-class primitives, zero domain components, zero routes, no import from the donor project. What stays live in the handoff is the audit-by-contradiction discipline, not the story.
## [2026-08-05] decision | **MAP-7's boundary decisions closed at pickup: the envelope's generation toolchain is researched and ratified first as its own ADR issue (MAP-31, blocks MAP-7), and MAP-7 carries the payload opaque with the target path typed per M9, the catalog staying MAP-8's, so the slice stays one tracer bullet (testing.md 1.2).** The toolchain became a decision because a hand-written Pydantic mirror of the envelope fails M8's one-definition acceptance by construction, and the survey's agenda item 7 had narrowed the open half to Rust-to-Dart without naming the Rust-to-Python direction the flush path forces.
## [2026-08-05] finding | **Three stale copies caught by the MAP-7 opening's fan-out grep, corrected with no decision moved and no version bumped.** PRD M10's heading, the Layer 3 batch preamble and M10's own acceptance still said four version axes after v0.11 ratified the fifth (the per-project version, ADR-0004), and M8's acceptance now reads "the four envelope-borne version axes" so it stops counting against a five-axis M10; index.md carried "Currently at v0.17/v0.15" against a disk at v0.17.1/v0.16, and the pointers were removed rather than refreshed, because a version pointer in a convenience catalog is a copy that has now drifted twice. Provenance untouched: the 2026-07-30 log line and SP-1's four-axis language are history.
## [2026-08-05] decision | **The envelope's generation toolchain is ADR-0009 (MAP-31): Rust the single source; schemars pinned to JSON Schema 2020-12 emitting the schema; datamodel-code-generator generating the Pydantic v2 model with its native check as the CI freshness gate; tsify landing the named TypeScript in the wasm-pack pkg, ts-rs into libs/contracts as the recorded exit path; the OpenAPI side references the core-generated type and never redeclares it; the Rust-to-Dart half stays open on the apps/mobile trigger.** The research round refuted two survey claims, corrected in dependencies.md section 2: Specta's Python and JSON Schema exporters are 0.0.x stubs on a core at 2.0.0-rc since 2023, and a serde value crosses wasm-bindgen as any, so Rust-to-TypeScript does not fall out of wasm-bindgen for a data contract.
## [2026-08-05] decision | **The MAP-7 Window A review closed three contract spellings with the owner: the applied operation carries M7's classification in force as the effective boolean (the derived record stays on the feature, per M7's own shape split, and a future derivation arrives as an additive field, never a repurpose); an absent mediation serializes as explicit null, because the envelope is self-describing (M8); and operation_type is a string in this slice, MAP-8 narrowing it to the closed catalog before any deployed consumer exists.** The boolean finding was reached independently by the Canon and Spec axes, blind to each other, which is what the isolated-axes design exists to buy.
## [2026-08-05] decision | **The testing.md section 6 granularity sentence is written, on its third bite: the requirement ID is named per test where a module's tests differ in what they cover, and once at the module where the whole module serves one set.** Decided in the MAP-5 round's discussion, left unwritten twice, surfaced again as cross-language variance by the MAP-7 Craft axis. The test skill's import remedy was corrected in the same pass: an import the implementation window will create is left red rather than ignore-anchored, because strict mypy's unused-ignore warning turns the anchor into an error the moment the module exists, forcing Window B to edit a test it may not touch.
## [2026-08-05] finding | **Three structural facts confirmed running the MAP-7 review, each against disk.** The api container sees only apps/api (infra/docker/api.Dockerfile COPY and the compose mount), so the one shared golden-corpus location testing.md section 4 requires cannot exist as built, blocking the C10/M13 corpus of the next slice; registered as MAP-32 with its trigger named. ng build web does not type-check spec files (tsconfig.app.json excludes them), so the TypeScript envelope contract is enforced by ng test rather than by the build. And the MAP-7 red lands exactly on the missing generated module in all three ecosystems, the state Window B resolves by generating it.
## [2026-08-06] decision | **The MAP-7 implementation review closed four calls with the owner, none blocking: the schema requires the mediation key (the reader refuses an envelope that omits it, instead of silently normalizing absence to a direct human write); the generated union members get readable names via a title per TargetPath variant plus --use-title-as-name, the flag joining ADR-0009 section 2's enumerated set with a dated note; ruff is pinned exactly in apps/api, which dependencies.md section 1's own row already prescribed; and Debug stays deliberately off ClientHalf with a trap comment, because an all-fields Debug would print the payload and session material the logging path must never carry (N5, N9).** A production-only fix round carries them plus the review's comment corrections; the tests stay byte-identical.
## [2026-08-06] finding | **The MAP-7 green round, verified by running: just check exit 0, both freshness gates mutation-tested by the orchestrator (a hand-drifted generated model and a drifted schema each turn the gate red), the discriminator present in the generated file, tests byte-identical to the reviewed red.** Three empirical facts entered dependencies.md section 2: the boolean-true schema appears only without a doc comment and the generator emits Any either way, so the schema_with escape hatch is unnecessary at these pins; schemars emits no discriminator keyword and the extend annotation supplies what datamodel-code-generator needs to route by tag; unsigned integers carry their floor (ge=0) and not their ceiling. The worst Craft finding was a false trap comment: the codegen mount path is not load-bearing (only the basename enters the generated header, proven by a byte-identical regeneration from a moved mount), while the real fragility, the output path governing ruff config discovery, was undocumented; corrected in the fix round.
## [2026-08-06] decision | **The MAP-7 fix round closed all nine review findings and one deviation was ratified: the required-but-nullable mediation key comes from the schema emitter's serialize contract (for_serialize), because the per-field required attribute strips the null and inlines the definition, refusing the explicit null the wire carries.** The round also caught and fixed a bug beyond its list, with the reviewer's sanction: contracts-generate truncated the committed schema before the emitter ran, so a crashed emitter destroyed the artifact; both recipes now write through a temp file and a broken emitter reports itself instead of "the Rust types moved". Verified closing the round: just check green, the freshness gate mutation-tested again after regeneration, contracts-generate exercised end to end as the restore path, and the three test files byte-identical from the red commit through green.
## [2026-08-06] finding | **Two advisories deliberately deferred closing MAP-7, recorded so they outlive the session.** Moving the datamodel-code-generator flag set into a [tool.datamodel-codegen] table in apps/api/pyproject.toml would collapse the nine-flag duplication between the justfile and ci.yml to one home (the capability is verified against 0.72.1); deferred because both copies diff the same committed artifact, so drift is a loud red gate rather than a silent fork, and reshuffling just-landed wiring buys tidiness, not correctness. And schemars rides unconditionally in a cdylib whose wasm size is a stated concern (PRD S4): the JsonSchema impls should be unreachable from any wasm-bindgen export and stripped by LTO plus wasm-opt, asserted from the linking model rather than measured; worth one wasm-size measurement before anyone reaches for a cargo feature split.
## [2026-08-06] decision | **MAP-8's boundary decisions closed at pickup, three calls registered before dispatch.** The set-geometry payload is a GeoJSON-shaped structure in the M5 storage frame inside the JSON envelope, an interim whose recorded exit is MAP-33 (the geometry-encoding ADR, milestone 4, trigger the first UI-core geometry crossing), on the same no-deployed-consumer argument that let operation_type ride as a string for one round; the M9 geometry-family refusal clause (typed refusal, flag and retain, C7/C13) is deferred to milestone 3 on the MAP-7/T5.1 precedent, because its retention half needs the flush machinery, checked again at MAP-10 pickup; and the no-conflict-by-target-path acceptance is pinned as a pure disjointness decision in libs/core, no Python twin and no golden corpus this slice (the corpus stays with the conflict slice, MAP-32). The T3.3 two-whole-geometries clause stays with the conflict slice by the project's own exclusion. The task spec is specs/tasks/MAP-8-operation-catalog.md.
## [2026-08-06] decision | **The ADR-0008 section 4 divergence is closed by the dated note, not the wire: moving an issue to In Progress is a human act at pickup through the MCP, and nothing moves an issue into In Progress automatically.** The linear-workflow skill's Status flow restatement was corrected in the same pass, the fan-out reaching the one enforceable copy that still carried the old letter.
## [2026-08-06] finding | **The MAP-8 Window A review closed BLOCKED, and the load-bearing fact is that three isolated axes found one complex from three angles: the red's new pins force ClientHalf into a discriminated union with a typed payload per member, while the standing MAP-7 suite pins the deliberately wide shape, so Window B would have no legal path to green.** Beyond the two opacity tests the window itself reported, the axes added what it had not seen: envelope.rs:100 stops compiling under the natural restructure (the operation_type string assertion), the Python extras-survive round-trip cannot hold without an extra-fields allowance the ADR-0009 section 2 flag set does not carry (widening it is an ADR revision no window takes alone), the three MAP-7 canonical fixtures pair feature.geometry.set with kind feature (the granularity the catalog now declares wrong) in all three languages, no delivered test refuses that mismatched pairing on the wire, and Python has no positive witness that the feature.create member survived the generator. Machine gates verified by running: red exactly as reported in all three ecosystems (3 failed 111 passed in Python, three E0432 in Rust, TS18046/TS2322/TS2578 on the web), everything else green, no existing test touched.
## [2026-08-06] decision | **Four verdicts close the MAP-8 review round, each registered before the correction dispatch.** The MAP-7 suite revision is authorized in the Window A correction round (retire the two payload-opacity pins, re-pair the three canonical fixtures, reframe what the union restructure breaks without weakening it), because the catalog's closure is the requirement change those tests ride on and testing.md says the test changes with it in the same pass; the session-material opacity pins stay, OQ-18 being open. The type-to-target-kind pairing is contract, not metadata: a mismatched pairing is refused with a typed error, written into PRD M9's acceptance the same day as a dated sharpening, no version bump, the next PRD round absorbs it. The Python feature.create positive-parse witness is sanctioned deliberately green at red, in the ADR-0009 section 5 guard class the freshness gate cannot see, the exception documented in-file. Adopted advisories ride the same round: per-test requirement IDs in catalog.rs, failure messages in the envelope.rs idiom, the ng-test gate named correctly, the restating docstring reduced to a pointer, the CATALOG assertion freed of declaration order, the two tautological TS runtime tests reframed or honestly named; the JsonObject extraction is deliberately deferred on the MAP-7 pattern. The T3.1 different-features disjointness test is adopted into the slice's acceptance record rather than removed as creep, because the conflict slice needs it and it rides the same pure function.
## [2026-08-06] decision | **The MAP-8 correction-round review closed with the two-contracts deadlock resolved and the four prior findings closed, one new blocking pair stood from the Canon axis, and the owner ruled the structural exit.** The type-to-target-kind pairing is enforced **structurally in the contract**: each catalog member's target is typed to its declared kind variant in the Rust source, the schema and both generated forms, so the generated Python reader refuses a mismatch with no validator and the TypeScript reader refuses it at compile time. The cascade is authorized by name: the two target-discriminator guard tests in test_generated_envelope.py re-pin their error types to what concrete variants emit (the guarded property restated as variant survival), the envelope variant-pattern test may adjust syntax without weakening, a Python pairing-refusal test and a TS mis-paired fixture join the red, and the stale test_generated_envelope docstring drops its retired payload-opacity clause. Adopted in the same micro-round: the nested ancestor-descendant disjointness case is recorded as **deferred to the conflict slice with T3.4** in the granularity module doc (nothing consumes are_disjoint in production this milestone); the retirement comments reduce to pointer form; the set-geometry catalog fixtures mint a fresh operation_id in all three ecosystems (an identifier is never reused, M3); the TS ring literal becomes one const and the reused outsideTheClosedCatalog name splits; a docstring line names where set-geometry member survival is witnessed; the reframed envelope test drops its payload assertion; the read() helper's Debug note becomes a pointer to the derive's own trap comment. The **CATALOG order is ratified canonical and append-only** as declared in-file, the const's rustdoc owning the rule once Window B writes it.
## [2026-08-06] finding | **The MAP-8 second correction round is approved: the structural pairing is pinned and the red re-delivered, verified by running and by a line-for-line diff audit against the authorization rather than by a fresh axis panel, because every change in the round was itemized by the previous panel's findings and the audit found nothing outside them.** Verified: three Rust test binaries red for the right reason (the three E0432 plus the five E0164 the newtype patterns force); Python 6 failed 109 passed, the two re-pinned variant-survival guards and the new pairing refusal among the six; four TypeScript errors, both @ts-expect-error directives reading unused while the contract is wide; every green gate green (ruff, format, lint-imports, mypy strict, cargo fmt, ng lint, ng build, contracts end to end); metric 6 passed isolated; the set-geometry fixtures minting a fresh operation_id in all three ecosystems. One contingency rides to Window B and is not a licence: the Python error-type predictions (literal_error, missing) are unverifiable until the generated shape exists, and a divergence is a finding reported back for the orchestrator to re-pin, never a test edit. The @ts-expect-error multi-line-comment probe entered dependencies.md section 2.
## [2026-08-06] decision | **Five verdicts close the MAP-8 implementation review's findings, registered before the fix dispatch.** The mis-paired-target TS test is corrected by the named-const rewrite (the target lifted to a const typed PropertyTarget, so the incompatibility lands at the declaration where the line-scoped directive sits; the excess-property firing order was the defect, measured by Window B four ways). The tsify flatten annotation is **ratified rather than treated as the ADR-0009 section 3 exit trigger** (dated note on the ADR; the defect and the serde struct-tag trap entered dependencies.md section 2 as measured evidence). TargetKind keeps its five rungs, mirroring M9's closed ladder rather than shipping a two-rung reading of a five-variant TargetPath. The OperationType-to-Operation drift risk (two enums over one member list, nothing linking them) is **recorded debt with its trigger, the next catalog member**, whose obvious witness is an operation_type accessor plus a test. And the schema narrowing is on record with its re-check: TargetPath, TenantTarget, ProjectTarget and LayerTarget left the schema root's reach, so the committed schema and the generated Python lost them, correct today with no Python consumer, and **checked again at MAP-9 and MAP-10 pickup**, because the conflict rule's input is the target path and the flush will want target types server-side.
## [2026-08-06] finding | **MAP-8 closes its implementation review PASS WITH FINDINGS and the adopted polish landed: Canon zero violations, Spec zero blocking, Craft's six advisories adopted, five delivered by the polish round.** One Canon observation for the grep record: PropertyTarget's property field is a free string, so the contract does not yet pin property equals geometry for the geometry member; seen, inside the closed kind-level pairing decision, left to the slice that types the property axis. Verified closing: just check exit 0 run by the orchestrator after each round, the mis-paired-directive mutation probe reproduced (a legalized pairing turns the directive unused at its own line), both freshness gates mutation-tested the same day, six of seven test modules byte-identical to the red at 3b6223f and the seventh carrying exactly the authorized fix. One general fact the polish round bought, in dependencies.md section 2: a doc comment on a serialized field is a contract change, because schemars lifts it into the schema description and the generator into Field descriptions, so a documentation-only framing never holds for a /// on a wire field. The F1 advisory (the spec-file module docstring matching its import line) did not run in the polish dispatch, correctly refused as outside Window B's role, and stays pending its own Window A dispatch.
## [2026-08-06] decision | **MAP-8 is Done: pull request #19 merged with rebase at 23:07Z, the seven commits on main in order (red at 623d328 before green at bc22227), the Linear automation moving Backlog to In Progress at pickup, In Review at the pull request, Done at the merge, nobody touching it.** Two operational facts worth the grep. A **GitHub Actions major outage** (confirmed on githubstatus.com, Actions and Pages down) swallowed the pull_request events for most of the evening: the ruleset held the merge on three Expected placeholders with no run enqueued anywhere, the close-and-reopen kick was applied, and when the platform recovered the checks ran green (run 31129529702) before the owner merged, so the wall was passed through and never around. And the **merge's push event to main was dropped in the outage window**: main's tip (ad690e8) carries no CI run of its own, while the content is what run 31129529702 verified on the pull request ref (the branch was cut from main's tip, so the rebase changed hashes and nothing else); the next push to main restores the per-commit chain, and no action is taken beyond this record.
## [2026-08-07] decision | **The comment rule was written five times and the toolkit's own fifth copy was the permissive one, which is the root cause of over-commenting and was found by surveying an external skill set rather than inside it.** `CLAUDE.md` (global and project), `implement` section 5 and `code-review/references/canon-checks.md` all say a comment is a **trap and not an explanation**; then `implement` step 2 and `test` step 2 both order the window to invoke `solid`, whose `references/clean-code.md` headline read "Only write comments to explain WHY, not WHAT or HOW" with a GOOD example. The window obeys the last and most permissive instruction, and "explains why" admits every line because every line has a why. **Measured before acting, and the symptom has not fired here yet:** 96 comment lines in 2,089 lines of `apps/api/mapsift` (4.6 percent) and every inline comment in `libs/core` is a legitimate trap, because all production code so far was written under the three-window protocol with an orchestrator running three review rounds. The defect is latent, and the safety catch is orchestrator attention, which does not scale. **FIVE EDITS.** `solid/references/clean-code.md` stops deciding and **points** at `CLAUDE.md` (one fact, one tier), keeping the rename-instead-of-commenting half. `solid/SKILL.md` gains a **precedence block naming five collisions**, chosen over surgical edits because the file is vendorized and a header survives a re-sync: comments, "ALWAYS wrap primitives in domain objects" against `implement` section 3 and the M12 generation cost, the object-calisthenics size numbers against the Craft axis reading smells as heuristics (`ClientHalf` carries nine fields because M8 says so), TDD ownership against the two-window split, and the AAA label comments. `implement` section 5 gains a **default of zero**, two mechanical tests (the `writing-for-agents` no-op test pointed at code, and a length ratio), a **citation budget of one per decision at the line whose shape it made surprising**, and a three-way contrast block whose surviving example is the real `geodesic_area_unsigned` trap in `libs/core/src/metric.rs`. `code-review` **Stage 3 gains the finding shape its Craft axis structurally could not satisfy**: a smell produces no wrong output, so the third mandatory element on that axis is the **replacement** rather than the failure, and a Craft finding with no replacement is an opinion and goes unstated. **Owner's ruling folded into the same edit (intensity is medium):** the replacement is judged on the next reader and never on the line count, a shorter form that hides a tradeoff names it or is not raised, and a construct the canon made deliberately verbose is not a finding, calibrated against four on disk (the five single-variant target enums of M9, the hand-spelled `#[tsify(type = ...)]` of ADR-0009 section 3, a closed set with one variant under M13, and an empty payload struct). **PROVENANCE:** the contrast-pair device and the replacement requirement are craft taken from `github.com/DietrichGebert/ponytail` (MIT, read 2026-08-07); the skill set itself was **rejected for installation**, its test rule being test-after without frameworks against `testing.md` section 9, its `ponytail:` marker being a second backlog inside code comments against ADR-0008, and its unscoped ruleset loading at tier 0 by accident against ADR-0002 section 5. Its `examples/` substitution catalog is **deferred with a named gate**: the first real install-a-package ruling becomes row one of `.claude/skills/implement/references/`, born with a verification date instead of copied.
## [2026-08-07] decision | **The Arrange-Act-Assert labels were three positions in one toolkit and the canon's own word invited it.** `specs/testing.md` section 2 said "in that order and visibly", `test/SKILL.md` said "visibly", `angular-testing/SKILL.md` said "**Do NOT include** `// Arrange` or `// Act` or `// Assert` comments", and `solid` showed the labels in a GOOD example in three files. Resolved toward `angular-testing`: the blocks are **separated by a blank line and never labelled**, because a label says what the code already says and is the naming failure the comment discipline forbids. `specs/testing.md` section 2 and `test/SKILL.md` now say so explicitly and the ambiguity that produced the split is gone. This was a second, independent source of the over-commenting symptom, generated by instruction rather than by carelessness, and it reached the test window as well as the implementation window.
## [2026-08-07] finding | **A state claim this log carried was false against disk, corrected here rather than by editing the entry that carries it.** The 2026-08-06 MAP-8 entry above says the merge's push event to main was dropped in the GitHub Actions outage window and that main's tip `ad690e8` carries no CI run of its own. Measured at the MAP-9 pickup (`gh run list --branch main --json databaseId,headSha,event,conclusion,createdAt`): run **31130512721**, `headSha=ad690e8`, `event=push`, **completed success at 2026-08-06T23:18:23Z**, eleven minutes after the 23:07Z merge. The event was **delayed by the outage backlog, not dropped**, and the per-commit chain never broke. The wrong claim was written while the backlog was still draining, which is the failure mode this project's own rule exists against: a state claim is written only with the command that verified it, and **re-checking is part of that command** when the system being measured is known to be degraded. The 2026-08-06 entry stays as written, because rewriting it would erase the fact that the claim was made.
## [2026-08-07] decision | **The per-project version is envelope-borne, on the server half, which reverses a count this canon set deliberately two days earlier.** Closed at the MAP-9 pickup as an in-place dated sharpening of PRD M8 (Shape, Acceptance and Provenance) plus M10's Shape, no version bump. On 2026-08-05 the MAP-7 opening narrowed M8's acceptance to "the four envelope-borne version axes" so it would stop counting against a five-axis M10; that reconciled the arithmetic without ever asking **where** the fifth axis travels. It travels in the envelope. ADR-0004 distributes an allocated range across the operations of a flush, so each applied operation carries one, and a resync stream is a stream of applied operations; leaving the ordering key outside the envelope would put it in a structure parallel to the contract that exists to make an operation self-describing, and the client would then advance its resync cursor by assumption, which is the shape C12 already forbids on the mutation-number axis for exactly the same reason. Measured input to the ruling: `grep -rn "project_version\|ProjectVersion"` returned nothing anywhere in the repository, so axis 1 did not exist in code at all.
## [2026-08-07] decision | **Each version axis becomes a distinct type rather than a distinct field name, bought with a probe instead of an argument, and the probe found the doc comment load-bearing a third time.** M10's acceptance ("no code path reads one as another") was carried by field names alone, which is the convention-only reading the MAP-8 review refused for the M9 pairing; and measured on disk the four axes came in **two pairs of identical primitive** (`mutation_number: u64` with `feature_version: u64`, `operation_schema_version: u32` with `conflict_rule_version: u32` and `applied_rule_version: u32`), so the compiler accepted the substitution. Probed at the pins (schemars 1.2.2, datamodel-code-generator 0.72.1 under the justfile's exact flags, tsify 0.5.6, mypy 2.3.0, pydantic 2.13.4): a `#[serde(transparent)]` newtype gives **real nominal protection in Rust and in Python** (`RootModel[int]`, mypy `--strict` raising `arg-type` on the substitution) with the **wire byte-identical** (bare integer in and out), and **nil protection in TypeScript** (`export type X = number` is a structural alias, mutually assignable with the primitive), which no mechanism closes today because a branded type would be a hand-written declaration of a generated type against ADR-0009 section 4 and ts-rs carries no branding either. **The trap the probe bought:** a newtype **without a `///` is inlined by schemars**, reaches no `$defs`, and generates as a bare `Annotated[int, ...]` indistinguishable from the primitive, so the doc comment is what makes the Python half exist at all. Probed side by side, one newtype documented and one not. All of it is in `dependencies.md` section 2; the Python read cost (`.root` at every access) is recorded there too.
## [2026-08-07] decision | **The M10 clause that a per-feature version never decreases is deferred to MAP-11 with a named trigger, because the behaviour that would make it true does not exist yet.** M10's acceptance has five clauses and only the first (the axes are distinct and no code path reads one as another) is reachable inside MAP-9's seam; dedup by mutation number, the typed resend on a gap, and the never-decreases guarantee all need the flush, and dedup and the gap already belong to MAP-12 and MAP-13. Writing the assertion at MAP-9 would be a test over imagined behaviour, which is the horizontal-slicing anti-pattern `specs/testing.md` section 1.2 names. The trigger is MAP-11, the slice that allocates the version.
## [2026-08-07] finding | **The entry above about the version-axis probe states the wrong mechanism, corrected here rather than by rewriting it, and the correction generalizes.** That entry says the trap is a newtype **without a `///`**, which mistook a correlation for a cause: the first probe carried `#[serde(transparent)]` on every newtype, so nobody separated the attribute from the doc comment. Re-probed as a full matrix at the same pins (2026-08-07, at the MAP-9 implementation review): a **plain** newtype reaches `$defs` documented or not and generates as `RootModel[int]` either way, and only a **`transparent`** one inlines when undocumented. `transparent` is redundant on a newtype struct anyway (serde already serializes it as its inner value, and removing it from all five left the emitted schema byte-identical), so the shipped rule is to declare an axis newtype plain, and the hazard survives only as the latent pair of adding `transparent` to an undocumented one. **Two further corrections came out of the same review, both of them over-claims of the same shape.** The Python nominal protection has a named hole that is this repository's own configuration: `[tool.pydantic-mypy] init_typed = false` makes a substitution passed **through a model constructor** silent, while the same substitution as a function argument or an annotated read still fires, so a distinctness assertion anchors on the read path. And **a generated contract's freshness gate is a staleness check and never a semantic one**: `just contracts` fails once on the stale artifact and goes green on the regeneration its own message tells you to run, because both gates compare a generated form against its source and neither can ask whether that source still means what it did, so the wall against a degraded axis is a test and not a gate. All three live in `dependencies.md` section 2. The pattern worth keeping: each was caught by a window measuring rather than obeying, and each was the orchestrator generalizing a measurement past the configuration it was taken in.
## [2026-08-07] finding | **A merge fires against the head the platform knows, not the head on disk, and pull request #21 dropped a commit to exactly that.** The branch carried four commits and the rebase replayed three: `20e84fc` was pushed while GitHub was still ingesting, `gh pr view 21` kept answering `headRefOid 0781e64` with `mergeable UNKNOWN`, **no CI run ever existed for that commit**, and the merge took the head the API reported. Recovered the same session as pull request #22, split into the two atomic commits its subject's "and" always implied, verified by `git diff --stat origin/main 20e84fc` returning empty. **The rule, and it costs one command:** before merging, compare the pull request's `headRefOid` against local `HEAD`; `mergeable UNKNOWN` is the platform saying it does not know yet rather than latency to wait out, and a state claim about a remote is written only with the command that verified it. **What was at stake rather than why it is tidy:** the dropped commit carried the `specs/log.md` line correcting the version-axis probe mechanism, so between the two merges `main` asserted a cause the re-probe had already refuted, which is the entry immediately above this one.
## [2026-08-07] decision | **The MAP-10 pickup closed six boundary decisions, and the first is that nobody owned the append-only operation log.** MAP-11 owns the version table by name, MAP-12 the per-clientID cursor, MAP-13 contiguity, MAP-14 the correlation keys, and PRD M15's log table had no issue at all, so **MAP-10 absorbs it**: a separate issue would be a table with no writer whose append-only test asserts against something nothing exercises. **The flush appends and does not write the projection**, because M15 leaves the materialization strategy (a maintained current-state table against replay on read) an open ADR and a flush that updates `Feature.geometry` would pick it in code and in silence. **The echoed last-applied cursor is MAP-12's** despite MAP-10's own acceptance text pointing at it, which is two issues claiming one clause and is fixed by a dated note on the tracker rather than by both building it. **MAP-10 dispatches alone with ADR-0004's LATE ordering fixed in the task spec as a constraint on shape**, so MAP-11 slots the allocation into a transaction already built to receive it, instead of the pair piling append-only, one transaction, the lock taken once, the narrow version table, the batch knob and the inherited never-decreases clause into one Window A. **Both armed re-checks come back negative as a consequence of the second decision**, the schema narrowing re-arming at the first slice that reads a target path server-side and the M9 geometry-family refusal gaining its retention half but not its refusal half.
## [2026-08-07] decision | **Append-only is enforced by privilege rather than watched by a test, and the runtime role's grant stops being one shape for every tenant-owned table.** PRD M15 asked for "no code path updates a log entry in place, asserted by a test"; ADR-0005 had already built four unprivileged roles, so `mapsift_app` taking **`SELECT, INSERT` and no `UPDATE, DELETE`** on the log makes the guarantee true at the same layer the isolation wall is, refused for every caller including a migration data-fix and a psql session rather than for the paths somebody remembered to cover. The privilege set is independent of row-level security, so the log keeps its policy and its tenant column and is not exempted from the wall. **The trap it creates is ADR-0005 section 4's silence generalized:** a refused in-place write surfaces as a *permission* error rather than a logical one, so the test must distinguish the grant denying it from a statement that matched no rows, since asserting only that nothing changed passes for the wrong reason under both. Registered in ADR-0005 section 2 and in M15's acceptance and provenance.
## [2026-08-07] finding | **The MAP-10 Window A review blocked, and its heaviest finding was a hole in the briefing rather than in the work: nothing said where a request's tenant comes from, so the window bound the wall from the value the wall polices.** No authenticated request seam exists anywhere in the stack (`config/api.py` builds `NinjaAPI` with no `auth=`, no tenant middleware, and `user_scope` plus `memberships_of_the_session_user` both have zero non-test callers), so the only implementation the delivered tests admitted reads the tenant out of the posted body and binds it, and the policy's `WITH CHECK` then passes because the row and the binding came from the same client-chosen value. **Two independent review axes reached it from different criteria**, which is the isolation the three-axis split exists to buy. It is **OWASP API1:2023, Broken Object Level Authorization**, in the first server-side write path of this product. **The research corrected the diagnosis rather than confirming it:** the defect is not that the target travels in the envelope, because PRD M8 requires exactly that and OWASP's own remedy is an authorization check on the received identifier rather than a ban on receiving one. The defect is that nothing verified the claim. Closed by **MAP-34**, which binds the authenticated principal and resolves the tenant by verifying it against the MAP-27 selector, and which now blocks MAP-10. The task spec's out-of-scope block gained the line whose absence caused this, and the general rule is that an out-of-scope block which omits where an input comes from is not a boundary, because a window cannot leave a required input unfilled.
## [2026-08-07] finding | **The append-only-by-privilege claim written this morning overstated its own reach, and the correction is the third instance this week of a measurement generalized past its configuration.** The ADR-0005 addition and PRD M15 said an in-place rewrite was refused "for every caller including a migration data-fix and a psql session". Measured on `layers_feature`: `mapsift_owner` holds `UPDATE`, `DELETE` and `TRUNCATE`, because a table's owner holds them implicitly and a `REVOKE` aimed at `mapsift_app` never reaches it, and migrations, tests and CI all connect as the owner (ADR-0005 decision 2). **The guarantee is the runtime path's**, and extending it to the owner is a separate and more expensive decision deliberately not taken. **`TRUNCATE` joins the grant in the same pass**, because it is a distinct privilege that row-level security does not apply to, so a role holding it empties every tenant's entries while `UPDATE` and `DELETE` stay absent, and a grant assertion enumerating four privileges and calling itself the whole grant misses exactly the one that defeats append-only. Both corrected in place in ADR-0005 section 2 and PRD M15 rather than rewritten silently.
## [2026-08-07] decision | **The authenticated request is ratified as ADR-0010, and the trigger that fired was the flush rather than login.** Django's session with CSRF on, over a **same-origin premise recorded as a requirement while what satisfies it in production stays open** (the dev-server proxy meets it today with no reverse proxy and no container; production waits on the same deployment-target trigger that holds back `infra/compose.prod.yaml`). The CSRF names stay Django's and the web client adapts in one line, because **CSRF is a browser concern only**: a Tauri webview inherits it unchanged and Flutter will never see a token. The mechanism lives at **one swappable point** with everything below it consuming the principal and never the mechanism, and the bearer-token exit is triggered by the first non-browser client, with the token's lifetime and rotation left where they belong in T5.2 and OQ-18. The tenant is read from the envelope and **verified** against the principal through the MAP-27 selector, never trusted and never duplicated into a path, and a claim the principal cannot back is refused as not-found before any binding, which is T6.5's cross-tenant half. **Performance was researched and deliberately did not decide it:** the CSRF check is an in-memory HMAC, the session costs one primary-key lookup, and a self-contained token would not avoid a lookup anyway because T5.2 requires the author's *current* authorization at flush; the one structural item is that `SESSION_ENGINE` defaults to `backends.db` and `cached_db` waits on Redis entering compose. ADR-0005 section 8's prediction held exactly: the endpoint consumes the selector and nothing about the wall moved.
## [2026-08-07] finding | **django-ninja's session authentication enforces CSRF, and Django's default test client hides that, which is the third distinct shape of self-reporting check this survey has caught.** `django_auth` is `SessionAuth` over `APIKeyCookie`, whose `__init__` carries `csrf: bool = True` and calls `check_csrf`. Measured at the pins: unauthenticated is `401` with no code of ours entered, so the unauthenticated clause is **asserted rather than implemented**; authenticated through the default `Client()` is `200`; the same request through `Client(enforce_csrf_checks=True)` is `403 CSRF check Failed`; with the token it is `200`. **A write test therefore uses the strict client**, because the default one goes green while the real browser POST is refused. Also measured and worth not rediscovering: the name mismatch between the two stacks (`csrftoken`/`HTTP_X_CSRFTOKEN` against `XSRF-TOKEN`/`X-XSRF-TOKEN`) carries **no security property**, since what secures the double-submit pattern is the cookie staying readable by same-origin JavaScript, a cross-origin page being unable to set a custom header, and `SameSite`.
## [2026-08-07] decision | **The MAP-34 Window A review ratified three contract facts the ADR had left for a window to invent, and the review is what found they were being invented.** ADR-0010 decision 6 gains a dated addition. **The route is `POST /api/operations` with the body `{"operations": [...]}`**, the named key rather than a bare array chosen because a bare `list[ClientHalf]` binds fine today (measured) but would have to break shape when MAP-12 adds the echoed cursor; the response body stays MAP-10's. **An empty batch is a second typed refusal and not a disagreement**, because a batch of no operations does not disagree with itself, it names no tenant at all, and collapsing the two makes different refusals indistinguishable to the client, which is the defect the decision already refuses on the first-wins read. And **not-found is a property of the whole response rather than of its status line**: T6.5's indistinguishability is comparative, so the testable form is that a claim on a real tenant the principal does not hold and a claim on a tenant that does not exist produce **the same response**, and anything differing between the two is the leak. The general lesson repeats the one from the MAP-10 round in a new costume: a decision that names an outcome without naming the shape that carries it leaves a window to invent the shape, and the window's invention becomes contract.
## [2026-08-07] finding | **A harness's silence is refuted by a positive assertion in the same suite, never by running against an implementation, and MAP-34's Window A bought that lesson by doing the second.** It installed a throwaway implementation, ran the 19 tests against it, reverted, and declared the deviation with its cost named. The risk was real: assertions of the form `bindings.tenants == []` all pass vacuously if the recorder captures nothing. The instrument was wrong, because the remedy already sat in the suite, in the positive assertions that must go red and then green (`bindings.tenants == [alice.tenant_id]`, `bindings.users == [bob.user_id]`); a recorder that captured nothing would hold those red through Window B. **The instinct found the right target anyway:** the one hole that survived is exactly the test carrying no positive control beside its empty-list assertion. Declared rather than hidden, so it is registered as authorized in retrospect rather than treated as a violation. **A second finding from the same review, and it generalizes past this task:** a witness over the tenant binding that records the parameter and its value but ignores `set_config`'s third argument cannot tell the transaction-scoped spelling from the session-scoped one, which is the exact leak ADR-0005 measurement D exists to catch, so a suite can claim to witness "transaction scoped" while being blind to it.
## [2026-08-07] decision | **Three contract facts the MAP-34 correction round left hanging are ratified in ADR-0010 decision 6's addition, and one of them was a window's derivation three tests already rested on.** **Both malformed-batch refusals answer `422` and are told apart by their bodies**, which is not a product decision: it follows from C5 because the batch is refused where Pydantic validates the boundary, it is django-ninja's own answer to a body violating its declared contract, and refusing at the boundary is what makes "nothing was bound" true by construction rather than by the diligence of whoever writes the handler. **The comparative not-found is compared over status and body and not headers**, because both requests are identical in shape so the same middleware sets the same headers, and comparing them would pin framework noise inside a test about a leak. **And exempting the availability probes is in scope for the seam**, even though the file lands in `config/`, because putting authentication on the API otherwise makes N12's liveness and readiness unreachable, which is a direct consequence of decision 4.
## [2026-08-07] finding | **A control that closes one arm of an instrument and claims to close both is the original defect surviving on the other side, and it took a mutation to prove rather than three agreeing reports.** MAP-34's Window A correction round added an instrument control for the binding recorder, executing the session-scoped spelling (ADR-0005 measurement D) and asserting the interpolated list empty beside it, with two docstrings stating both wrong spellings were executed. The interpolated spelling (measurement E) is executed nowhere in the repository, so that assertion rests on a control that does not cover it. **Confirmed by mutation rather than by agreement:** deleting `opened.interpolated.append(parameter)` from the recorder leaves the suite at exactly the same 21 failures, so nothing catches its removal. Three review axes reached it from three different criteria, which is the property the isolated-axis split exists to buy; the mutation is what turned concurrence into proof, and it is the cheap instrument to reach for whenever a suite claims to witness something. **A second limitation was found and deliberately not fixed:** the recorder reads the first `set_config(` in a statement and takes its value positionally, so a single statement binding both parameters is mis-attributed, which every axis confirmed fails toward red rather than toward false green. It is named in the docstring instead, because the structural-performance rule actively invites that one-round-trip spelling.
## [2026-08-07] finding | **The orchestrator wrote a log entry claiming ADR-0010 ratified something ADR-0010 did not contain, and the review caught it by grepping the document the entry pointed at.** The entry said three contract facts were ratified in decision 6's addition; the addition carried two of them, and the availability-probe exemption lived only in the MAP-34 task spec. **Corrected by writing the rule into the ADR rather than by narrowing the log line**, because the question it answers, which routes may be published without a credential, is one the next task greps for and a closed task's spec is not where anybody looks. This is the fan-out defect the canon rule names, committed by the person enforcing it, and the mechanism was the ordinary one: the decision was taken in conversation, written into the document nearest the work, and the summary was written from memory of the decision rather than from the document. **The cheap guard, which is what generalizes: a log entry that cites a document is written by opening that document, not by recalling what was put in it.**
## [2026-08-07] finding | **A docstring that makes a falsifiable claim is a claim, and needs the discipline an assertion gets; MAP-34 proved it four times in one task.** The correction rounds went behaviour, behaviour, text, text: a control that covered one arm of an instrument while three docstrings said it covered both; a measured fact stated backwards against the constant it asserted against; a "green on the day it was written" marker on a test that was red, in a repository where that phrase is a greppable marker for tests nothing drives; a docstring crediting one test with a guarantee that takes a pair, wrong both about what the assertion detects and about what would break; and an ADR citation naming the wrong decision, which the orchestrator had committed in the log one round earlier. **None of it was sloppiness and that is the point:** this window writes docstrings that assert rather than describe, which is the better habit, and the cost is that they carry the same failure mode as code. **The instruments that caught them are cheap and worth reaching for by default:** mutation for a claim about what a test detects (delete the arm, count the failures), a grep for a claim about an identifier or a marker, and opening the cited document for a claim about the canon. The generalization past this task is that **prose in a test file is reviewed against the body the way the body is reviewed against the requirement**, and a claim nobody can check in one command is a claim to rewrite as one that can be.
## [2026-08-07] finding | **A file stated a false security rule and cited an ADR for it, and the three review axes reached the same three lines from three different criteria.** `config/probes.py` gained a docstring saying the two availability probes are "the whole published surface" exempted from the credential. Measured against the running API: `/api/openapi.json` and `/api/docs` also answer 200 without one, because django-ninja registers them as URL patterns rather than as operations, so they can never appear in the OpenAPI document the enumeration test reads and that test is blind to them by construction. The docstring is therefore **a restatement of ADR-0010's argument wrapped around a claim ADR-0010 never makes and that is not true**, in the same task whose own log already records the fan-out defect twice. Filed as **MAP-36** because the underlying gap is pre-existing and is a real decision (whether a schema describing every route of a multi-tenant system should be published without a credential at all), while the false sentence is corrected here. **The pattern worth carrying: a sentence asserting the completeness of a set is a claim about everything outside the set, which is the hardest kind to check and the easiest to write.**
## [2026-08-07] finding | **The seam's principal is a structural contract that type-checks every consumer and no supplier, and the safety it rests on today evaporates exactly when ADR-0010 decision 5's trigger fires.** The framework assigns the authenticated identity dynamically and types it loosely, so nothing binds what it yields to what the seam declares it requires. It holds today by inspection and is pinned at runtime only as a side effect, because the suite's binding recorder parses the bound value and a non-conforming identifier raises there. Two independent axes reached the same conclusion about the remedy, that a conformance assertion today is more ceremony than the gap is worth, and both named the same reason it stops being true later: a token implementation returning a record, a string or a mapping type-checks everywhere, and a session-cookie suite exercises none of that path. Recorded as a dated note inside decision 5 rather than as work, so the obligation travels with the trigger instead of with somebody's memory.
## [2026-08-10] decision | **MAP-10 resumes on a rebased branch and gives up the word in its own title: the atomicity assertion moves to MAP-11, because MAP-34 closed the last failure that could reach it.** Three boundary decisions at the resumption, registered before any window. **The atomicity assertion is MAP-11's**, and the argument is mechanical rather than a preference: ADR-0010 decision 6 refuses a batch disagreeing on its tenant at the Pydantic boundary before any binding, so the cross-tenant operation the first Window A used as its mid-batch failure no longer reaches the wall through the route; nothing runs after the append in this slice; the log holds no foreign key to a layer or a feature; and a duplicate `operation_id` fails a single `INSERT` whole, which a non-transactional implementation survives identically. MAP-11 puts the version allocation after the append, which is the second statement a failure can roll the first one back from. **The log carries `UNIQUE (tenant_id, operation_id)`** as structural integrity, distinct from MAP-12's `(client_id, mutation_number)` dedup across batches, led by the tenant per the wall's index rule. **And the flush answers with no body in this slice**, because the only thing a client needs to advance its cursor is MAP-12's echoed last-applied and ADR-0010 decision 6's addition deliberately left the response shape open. **The finding that made the first decision cheap:** PRD T2.2's *acceptance* never carried an atomicity clause at all (its two clauses are gap detection and resync, which is MAP-22's, and no authoritative state read from the WebSocket tier); "transactional API call" lives in T2.2's *Requirement*. The clause was invented by the task spec, which cites and never adds, so the task spec was the wrong document and no canon is weakened by striking it. **The generalization, which is the fan-out rule pointed at a task spec rather than at a document:** a spec-per-task that writes an acceptance bullet its cited requirement does not carry has created a requirement outside the fan-out, and the test written against it is the "test with no requirement" `specs/testing.md` section 6 says to delete.
## [2026-08-10] finding | **A task blocked across a sibling's merge comes back with its tests contradicting a decision that did not exist when they were written, and the orchestrator boot is what has to catch it.** MAP-10's Window A landed 2026-08-07 before ADR-0010; MAP-34 then fixed the route as `POST /api/operations` with the body `{"operations": [...]}`, measured that Django's default test `Client()` hides django-ninja's CSRF check, and put `a_browser`, `a_feature_create_claiming` and `a_geometry_set_claiming` into `apps/api/conftest.py`. On resumption the branch's `test_flush.py` still posted a bare array to `/api/sync/flush` with the default client, and `mapsift/sync/tests/envelopes.py` was a second copy of a conftest builder. **None of that is in the four blocking findings the review recorded**, because all four predate the decision that invalidated them; the live-state block inherited the four and said nothing about the three. **The cheap guard, and it costs one diff:** a branch resumed after a sibling merged is read against the canon as it stands now, not against the review that blocked it, and the boot reports the delta as a divergence the same way it reports a stale document. **Two smaller state corrections from the same boot:** the MAP-10 task spec did not exist on `main` at all (it lives only on the feature branch, so the live state's "eight specs-per-task, all catalogued in `specs/index.md`" was false, seven being the number on `main`), and the spec's own on-disk measurement of `mapsift/sync/` was taken before MAP-34 added `api.py`, `rules.py` and `tests/` to it.
## [2026-08-10] finding | **A collection error interrupts the whole pytest run, so the two-window red phase is a state in which nothing else in the suite is being executed either, and a tally reported from a flag is not the gate's answer.** MAP-10's Window A reported `147 passed, 2 errors in 10.59s`. Measured by the orchestrator on the same tree: plain `pytest`, which is what `just test` runs, answers `Interrupted: 2 errors during collection` in 0.31s and **runs none of the other 147**. The reported number reproduces exactly under `--continue-on-collection-errors`, which is in neither `addopts` nor the recipe, so the measurement is true and its configuration went unstated, which is the third instance this week of a number arriving without the configuration that produced it. **The conclusion the number carried was correct** and was independently confirmed, so this is a defect in the record rather than in the work. **What generalizes past the reporting, and is not written anywhere in the canon:** during a Window A red phase whose red is at *import* rather than at assertion, the rest of the suite is not a safety net, because it is not running. A regression introduced anywhere in `apps/api` between the red commit and the green one is invisible until Window B makes the module importable. The cheap guard is that the orchestrator's review runs the suite with the flag **and** without it, and reports both, because each answers a different question: with it, did anything else break; without it, what does the gate actually say.
## [2026-08-10] finding | **The orchestrator over-specified a boundary decision into an acceptance clause, and the window was right to assert behaviour instead.** Boundary decision 8 of MAP-10 named the constraint (`UNIQUE (tenant_id, operation_id)`) inside an acceptance bullet, and the review opened with a drafted blocking finding that the delivered test could not tell that constraint from a global one, which is the cross-tenant uniqueness channel ADR-0005 probe I exists for. **The finding died on opening the file it was about.** `tests/test_tenant_isolation.py` drives every tenant-owned table through a catalogue case refusing any non-primary unique index whose leading column is not `tenant_id`, so the tenant half is held by construction the moment the table carries the column, which is what the log suite's first test pins. The delivered test asserts the SQLSTATE, which separates a database refusal from a Python guard, and asserts nothing about the constraint's spelling, which is `specs/testing.md` section 2 obeyed correctly. **The correction is to the decision, not to the test:** a constraint spelling is a shape instruction for the implementing window, the way ADR-0004's LATE ordering already is, and putting it in an acceptance bullet invites a test that pins implementation. **The instrument that saved it is the one this log already names:** a claim about what a suite covers is checked by opening that suite, and the review that drafts a finding from the diff alone is reasoning where it should be measuring.
## [2026-08-10] finding | **The append-only log went in carrying an author claim nobody verifies, and because it is append-only that row can only ever be appended over, so the gap was armed as MAP-37 at the implementation review rather than when it hurts.** MAP-10's handler stores the envelope as the client authored it, which is exactly what M15's Shape requires, and that envelope carries `author_session_material`. C13 requires the authoritative identity to be the authenticated session that created the operation, normalized by the server at flush, with a divergence normalized to the proven identity or rejected and retained. **Nothing does that, and no issue among the thirty-six owned it.** MAP-10 is not defective for it: the acceptance the orchestrator wrote read C13 as the tenant half only, so the defect is in the acceptance and the window implemented what it was given. **What makes the timing the point rather than the omission:** an append-only table cannot be corrected, so every operation written before the normalization exists carries an unverified claim permanently, and the only reason this is High rather than Urgent is that nothing writes real data today. **The generalization, and it is the same shape as the round that blocked this task in the first place:** a task spec's Out of scope block is where an invariant's unaddressed half has to be named, because a window fills a vacuum with whatever the requirement in front of it admits, and here what it admitted was correct and incomplete at once. The line is now in the spec and not only in the tracker.
## [2026-08-10] finding | **Django writes migrations through black when black is importable, black arrived as somebody else's transitive dependency, and no gate in this repository can see the resulting split.** `django.db.migrations.writer` formats with black when it can import it. Nobody chose black here: it entered the api image with **datamodel-code-generator** at ADR-0009 time, and it imports at 26.5.1 (verified 2026-08-10, not assumed). So the two migrations written before that date carry Django's raw long-line output and every one written after carries black's, in the same tree. **`extend-exclude = ["migrations"]` under `[tool.ruff]` means neither `ruff check` nor `ruff format --check` reads them** (verified), so nothing reports the split and a reader who finds it later cannot date it from the files. Recorded in `dependencies.md` section 1. The rule is unchanged, that the generator writes what the installed version produces and its output is never hand-reformatted; what is written down is why one generator produces two shapes, so nobody goes looking for a decision that was never taken.
## [2026-08-10] finding | **A regenerated initial migration keeps its name, so a database that already applied the old one never re-runs it and ends up silently disagreeing with the file, and no gate in this repository compares those two.** Found at MAP-10's correction round, where the ruling to drop `editable=False` from two fields was applied the way `.claude/rules/python-django.md` requires, by deleting `sync/0001_initial.py` and regenerating it rather than hand-editing two lines. The regenerated file is still `sync.0001_initial`, and Django's migration recorder keys on app label plus name, so the development volume that had applied the previous copy did not notice. **It was harmless here for a reason worth stating rather than assuming, and it was measured: `editable` emits no DDL at all.** `manage.py sqlmigrate sync 0001` produces a `CREATE TABLE` with no trace of it, so both copies build the identical schema (the same command is what confirmed the unique constraint leads with `tenant_id` in the database and not only in `fields=[...]`). **With any option that does emit DDL the outcome inverts:** the database keeps the old shape, the file claims the new one, and `makemigrations --check` still passes, because it compares the **model** against the **migration** and never the migration against the database. **This is the third costume of a rule this log already carries twice: a gate that compares an artifact against its source is a staleness check and never a semantic one.** The cheap guard, when an already-applied initial migration is regenerated with anything that touches the schema, is to drop the volume rather than to trust the recorder, because the recorder is not lying, it is answering a different question.
## [2026-08-10] finding | **A fan-out sweep found five pre-scaffold sentences that outlived the scaffold, in four documents including the foundation, and every one of them was a pointer claiming something did not exist yet.** Corrections, not decisions, so they are fixed in place with no version bump (`fan-out` skill section 1). The foundation section 14 and `CLAUDE.md` both called `specs/testing.md` "to be created" while it is on disk at 17,681 bytes and is the document the whole three-window protocol cites. `.claude/rules/python-django.md` said "nothing is pinned yet (no lockfile exists until the scaffold)" and `.claude/rules/angular.md` said "`apps/web` does not exist and `specs/dependencies.md` is still to be written", with all three lockfiles (`apps/api/uv.lock`, `libs/core/Cargo.lock`, `package-lock.json`), `apps/web` and the survey all present. And `dependencies.md`'s own header said "no lockfile exists yet because the scaffold does not exist. Everything below is therefore a survey finding, not a pin", **while entries below it cite pinned numbers by name** (django-ninja 1.6.2, pydantic 2.13.4, Django 5.2.16), so the document contradicted itself. **What generalizes: the most durable stale claim is a negative one.** A sentence saying a thing does not exist yet is never revisited by the work that creates the thing, because creating it touches the new file and not the four documents that mentioned its absence, and a grep for the new name finds none of them. The sweep that finds them greps for the **absence phrasing** ("to be created", "does not exist", "nothing is pinned yet", "until the scaffold"), which is the second grep the `fan-out` skill already prescribes and which is the one people skip. **Left deliberately unedited and reported instead:** the foundation section 1 and `CLAUDE.md` both still say "no product capability is built yet", which is a characterisation the owner holds rather than a pointer, and is arguable rather than false while no user can yet draw anything.
## [2026-08-10] finding | **A merged pull request reports `mergeable: UNKNOWN`, which is the same string an open one reports while the platform is still computing, and this canon already carried a rule that reads that string the other way.** At MAP-10's close the orchestrator queried pull request 25, got `MERGEABLE` once, then `UNKNOWN` on every later query, and applied the rule from the PR-21 scar of 2026-08-07: `mergeable UNKNOWN` is the platform saying it does not know yet rather than latency to wait out. It polled six times and would have polled forever. **The pull request had already been merged by the owner**, and for a merged pull request the mergeability question is moot, so the API answers `UNKNOWN` because there is nothing to compute rather than because it has not computed yet. **The refinement, and it costs nothing: read `state` before reading `mergeable`.** `UNKNOWN` on an `OPEN` pull request is the PR-21 case and means wait and re-check; `UNKNOWN` on a `MERGED` one means the field no longer applies. The older rule stays exactly as written, because it is right about the case it describes; what was wrong was applying it without checking which case this was. **The general shape is one this log keeps meeting from new angles: a rule learned from one failure is applied to a symptom it did not come from, and the tell is that the remedy does not converge.** Six identical answers to the same question is the signal to ask a different question rather than to ask that one again.
## [2026-08-10] decision | **One flush addresses exactly one project, and the rule is the enforcement of a consequence taken in ADR-0004 and never written down.** Closed at the MAP-11 pickup and recorded in **ADR-0010 decision 6** beside the tenant refusal, with a dated pointer from ADR-0004's Consequences. ADR-0010 decision 6 already cited "ADR-0004 already makes cross-project atomicity inexpressible" as one of the two things forcing one tenant per flush, then fixed the refusal on the tenant alone; the project half stayed implied and unenforced, which cost nothing while no code took a per-project lock and stops costing nothing at MAP-11, where the ADR-0004 allocation **is** that lock. A batch across two projects takes two locks, restoring the repeated acquisition the RANGE rule removes and opening a deadlock between flushes that order their locks differently, and one transaction over both makes the batch **atomic across projects**, which ADR-0004 states is not expressible. The refusal is the **third typed `MalformedBatch`**, at the Pydantic boundary, `422`, told apart by its body. **What generalizes: a latent contradiction is not a document disagreeing with another, it is a document using a premise as an argument and then not carrying the premise's own consequence.** A grep for the rule finds nothing, because the rule was never written; a grep for the premise finds it being cited. Deliberately unanswered: an operation whose target carries no project, unreachable while the closed catalog declares no tenant-granularity member.
## [2026-08-10] decision | **The per-project version reaches disk as a column beside the log entry, never as a stored server half.** Closed at the MAP-11 pickup and recorded in **ADR-0004 decision 4**. PRD M8 puts the axis on the envelope's server half and says nothing about storage, because PRD Layer 3 fixes the shape and never the table. Two reasons converge. The read this axis exists for is the resync, `WHERE tenant = ... AND project_id = ... AND project_version > cursor ORDER BY project_version`, which wants a b-tree over a real column rather than an extraction from a JSON document, and the log carries no project column at all today because the address sits inside the client half it stores verbatim. And **four of the server half's six fields belong to work nobody has decided**: the per-feature version, the applied rule version, M7's legal weight in force behind OQ-8, and a verdict set with one member, so persisting a server half now persists a document that is mostly a guess about those four, which `specs/testing.md` section 7 calls an unbuilt future. The entry takes `project_id` and `project_version` as columns; the whole server half assembles at the read boundary when its remaining fields have owners.
## [2026-08-10] decision | **The per-project version table holds `SELECT, INSERT, UPDATE`, and the pair with the append-only log establishes that a grant is decided per table by what that table guarantees.** Closed at the MAP-11 pickup and recorded in **ADR-0005 section 2**, beside the addition of 2026-08-07 that narrowed the log to `SELECT, INSERT`. The version row's normal operation **is** an in-place increment, so anything narrower stops a flush running; the log's guarantee is untouched, because they are different tables. Written down rather than left to inference for a specific reason: after 2026-08-07 an `UPDATE` grant in this repository reads against the newest habit and will eventually be flagged as the defect the log's grant was. The rule the pair leaves behind is what the original bullet did not carry, since it stated one grant shape for "the tenant-owned tables" as a class.
## [2026-08-10] decision | **The MAP-9 clause "a per-feature version never decreases" leaves MAP-11 for **MAP-38** in the same milestone, and this is the second time that clause has been deferred.** Closed at the MAP-11 pickup; the canon does not move, because PRD M10's acceptance is unchanged and only which issue discharges it does. The reason is `specs/testing.md` section 1.2 rather than convenience: the two axes share no seam. The per-project version is one counter and one row locked per flush; the per-feature version is a counter per feature and N rows locked per flush, and **it has no storage anywhere today**, since `layers_feature` carries no version column and the log carries none, so deciding where it lives runs into the operation-log projection-strategy ADR that still has no issue. A window asked for both would be writing tests for a machine it cannot picture, which is the horizontal-slicing tell 1.2 says is visible in the scope block before anything runs. It stays in milestone 3 rather than moving to the conflict slice that is its only consumer, because the mechanism is flush-path work and because leaving one of M10's four acceptance clauses to a project nobody has planned is how a requirement gets lost at a planning boundary.
## [2026-08-10] decision | **Three `hono` advisories were acted on where a near-identical one was refused six days earlier, and the pair establishes that exposure and remedy are assessed separately.** Recorded in `specs/dependencies.md` section 5.9, whose heading changed from "assessed and deliberately not acted on" in the same pass, because a heading is a claim about its contents and that one had gone false. The three (GHSA-f23p-vx2j-j53r and GHSA-54fx-42gc-7vw4 medium, GHSA-79qm-7rj5-m7r9 low, all patched in 4.12.34) carry **zero exposure on the same three grounds the 2026-08-04 entry used**: `development` scope by Dependabot's own classification, `hono` reaching this tree only through `@modelcontextprotocol/sdk` and `@hono/node-server` so it enters no bundle and runs on no server of ours, and no caller for Hono SSR, its language middleware or its proxy helper anywhere here. **What flipped the verdict is the cost of the remedy, not the risk.** The 2026-08-04 refusal was bought by a forced `@angular/cli` downgrade from 22.1.2 to 21.0.4, which ADR-0002 forbids in spirit, or an `overrides` pin across a major; this one is a single package and a single version line, 4.12.33 to 4.13.1, a minor inside the `^4` already declared. **A free remedy for a zero exposure is still worth taking, because standing alerts on the default branch are the noise that makes the next real one easy to ignore.** Merged as pull request 24; open alerts went from three to zero. The `@hono/node-server` advisory is a different package and stays dismissed with its trigger intact.
## [2026-08-10] decision | **The per-project counter row is created on first use and does not reference the project table, and the question was never open even though the task spec said it was.** Recorded in **ADR-0004 decision 4**, second addition of 2026-08-10, ruled at MAP-11's Window A review. **Two measurements closed it, and neither was a preference.** Nothing in `apps/api` creates a `Project` at all (`mapsift/accounts/services.py` publishes account creation and no project creation), so **eager creation has no place to live** and a hook invented for it would be a service written to satisfy a counter rather than a use case. And the flush suites that predate the task post batches addressing a project with no row in `accounts_project`, which an implementing window may not edit, so the allocation must succeed for a project the table has never seen; that rules out a foreign key, which ADR-0005 section 5 would additionally have forced to be composite. **What generalizes, and it is the second time this week from a different angle: a decision recorded as open can already be closed by an artifact nobody consulted.** The task spec called this the implementing window's call; a fixture default and three older tests had decided it first. The cost of the error was zero only because the review ran the greps; a window would have discovered it by going red against tests it was forbidden to touch. **Accepted consequence, written rather than found later:** a counter row can exist for a project identifier that never did, and nothing in this strategy asked the table to prove otherwise.
## [2026-08-10] decision | **A batch-bound acceptance clause was struck from MAP-11 and moved to MAP-22, because it had no consumer where it sat.** "Batch size is a declared knob, starting at 500" travelled from ADR-0004 decision 3 into MAP-11's issue and from there into its task spec, and **PRD N10 puts the bounding on the client**, which flushes a large queue in bounded batches with per-batch acknowledgement. Nothing on the server reads it, so the symbol would have had no caller and the only test it admits asserts that a constant equals a constant. Window A wrote it honestly and flagged it as unassertable, which is the report that surfaced the defect; **striking an acceptance clause is the orchestrator's and never a window's**, which is why it came back rather than being sharpened in place. It lands on MAP-22, the one issue already tracing N10. **The generalization: an acceptance clause copied down the chain inherits its wording but not its consumer**, and the check that catches it is asking which code reads the value, not whether the citation is real. Giving the bound teeth on the server instead would be a fourth typed refusal in ADR-0010, that is, inventing behaviour to justify a test.
## [2026-08-10] decision | **ADR-0004's LATE rule was being read backwards, and the backwards reading reached a test; decision 2 is sharpened and the atomicity clause inverts.** LATE is a claim about the **critical section** being last, not about the order of the two statements inside it, and decision 2's own closing sentence already gave that order: "under the lock there is one allocation and one bulk insert". MAP-11's issue, its task spec in three places, and Window A's test all read "the allocation is the last thing before commit" as "the insert precedes the allocation". **What makes this more than a wording slip is that the order stopped being a preference on the same day: decision 4's addition made the per-project version a column on the log row, and `mapsift_app` holds `SELECT, INSERT` on that table and no `UPDATE`, so nothing can stamp a version onto a row already written.** The allocation runs first, by construction. The clause's reachable form is therefore the mirror of what was written: a failure in the append rolls back the allocation that preceded it. **The generalization, and it is the exact mirror of the one logged hours earlier about removing a constraint: a decision that ADDS a field has to be read against every ordering clause written before that field existed.** Adding a column is the cheapest-looking kind of change and it silently re-times everything that has to be present at insert.
## [2026-08-10] finding | **The first real run of the three isolated code-review axes found twenty-four things in the commit that created them, and five were defects that made the new guards refuse legitimate work.** `.claude/skills/README.md` had prescribed subagents for these axes since the folder was audited and nobody had ever run them that way, so the property the design leans on was unverified. Run against MAP-40 and MAP-41: **seven blocking, seventeen advisory, none of them in the orchestrator's own two readings of the same diff.** **The three found the missing test suite independently, by three different routes, and were otherwise disjoint.** Spec found the defects **inside the scripts**, and found them because one of its own calls was blocked by the hook under review. Canon found the defects **in the authority chain**: a mechanism that terminates a tool call had amended no ADR, while the same branch had correctly amended ADR-0008 before touching a skill. Craft found what **only running proves**, including a guard that refused `git rebase origin/main && git push --force-with-lease`, the recovery `dev-workflow` section 5 prescribes, and one that refused every edit to `README.md` because a shields.io badge escapes a literal hyphen as a double one. **Neither axis would have found the other's.** The separation is recorded here as a measurement rather than as a principle, and the five shipped defects are now REGRESSION cases in `.claude/hooks/hooks-test.sh`, each of which passed before the fix.
## [2026-08-10] decision | **A window may be opened by the orchestrator or by the owner, automatic is the default, and the refusal that looked like it forbade this was scoped rather than reversed (MAP-41).** ADR-0008 section 4 gained both modes and section 7 gained a scoping note. **The refusal stands exactly as written for what it named**, a webhook bridge turning an issue assignment into a run that opens a pull request unattended, and its stated reason is that this **collapses the two windows into one pass**. Orchestrated dispatch does the opposite: it isolates them, and the orchestrator's review still sits between Window A and Window B. **The method had never specified who opens the window**, so the manual mode had become the method rather than a mode of it, and it is now `orchestrate-manual`. **Three conditions, each a fact on disk rather than a judgement of the day:** a task spec with its boundary decisions registered, the gate running from the orchestrator, and the enforcement layer existing at all. **What the automatic mode costs is not isolation but the chance to interrupt**, which is worth little once the contract is written and a great deal while the task is still being understood, and that is the condition that selects the manual one. The three `code-review` axes became three parallel subagents by default in the same round, with their prompts versioned at `.claude/skills/code-review/references/axis-prompts.md` rather than composed per run, on the argument that moved the window protocol's standing discipline into `test` and `implement`: what repeats across dispatches belongs in a file.
## [2026-08-10] decision | **The canon got its enforcement layer, and the three properties that make an unattended subagent safe were measured rather than assumed (MAP-40).** Three hooks at `.claude/hooks/`, wired in `.claude/settings.json`: `check-prose.sh` refuses the em dash rule at write time, `block-main-push.sh` refuses a push touching `main`, and `block-production-secrets.sh` refuses a credential written or staged and refuses `git add -A`, which `dev-workflow` section 4 has forbidden with nothing enforcing it. **`.claude/skills/README.md` had recorded the absence by name as "a known gap rather than a decision" since the folder was audited**, and it was defensible only while a human sat between every instruction and every file. **What was measured, with a probe subagent:** the root `CLAUDE.md` is inherited before the subagent reads anything, so tier 0 survives isolation; a path-scoped rule arrives **on demand**, delivered alongside the `Read` that matches its glob and only the matching one, so tier 1 survives lazily; and before this change a probe wrote an em dash to disk **unintercepted**, which is what the gap looked like from inside. **Two findings came out of building it.** The hooks fire **without a session restart**, learned because the first secrets hook blocked its own test command minutes after being written. And that block was a real defect: it matched `git add` anywhere in the command text, so a payload quoting the string was treated as a command. **A guard wider than its rule gets switched off, which is worse than not having it**, so the match is anchored at a command position now. All fifteen cases are tripped deliberately by a suite, because a hook nobody has defeated on purpose is a hook nobody has tested.
## [2026-08-10] finding | **A rule written to make another rule checkable was itself over-broad within hours, and the implementing window is what caught it.** ADR-0004 decision 2's sharpening gained a self-check reading "does anything between the allocation and the commit scale with the batch". **That condemns the distribution the RANGE rule itself prescribes**: a flush "distributes the N versions internally", the range can only be handed out after the allocation answers, so the loop assigning it is the decision's own instruction. The window implemented the precise half correctly, refused to claim nothing inside scales, and **measured the difference instead of arguing it**: on a 500-operation batch, building the entries is 11.99 ms and stamping the range is 0.05 ms, a factor of 222. The check is now per-operation *work* beyond handing out the range, not per-operation *anything*. **What generalizes: a catchy restatement written beside a precise one gets read instead of it.** The precise sentence named three things (serialising a payload, resolving an address, building a model instance) and was right; the memorable sentence beside it was wrong, and a memorable sentence is what a reader carries away. If a rule needs a slogan to be usable, the slogan is part of the rule and takes the same scrutiny as the clause.
## [2026-08-10] finding | **An implementation satisfied every case written for the LATE rule and still grew the critical section with the batch, so the rule gained an operational form.** Caught at MAP-11's implementation review. The allocation was correctly first and the flush touched the counter exactly once, but the log entries were built in a **generator passed to `bulk_create`**, which is consumed inside the lock rather than before it, so a 500-operation flush would hold one project's version row across 500 `model_dump` calls instead of across one `INSERT`. **That is precisely the cost the two rules were measured to remove**: the spike's 56-fold improvement on the worst interactive save came from not holding the lock across per-operation work. Recorded in ADR-0004 decision 2's sharpening: no per-operation work runs between the allocation and the insert, and the self-check is not "is the allocation last" but **"does anything between the allocation and the commit scale with the batch"**. **What generalizes, and it is why this took an implementation to find: laziness moves work across a boundary without moving any code.** A generator expression reads as though it runs where it is written and runs where it is consumed, so a reviewer checking statement order sees the right order. **The suite cannot see it either**, since the lock-count case counts statements and not what happens between them, which makes this the second property of this decision enforced by reading rather than by the build.
## [2026-08-10] finding | **The shortcut that satisfies every case written for the per-project version and reintroduces silent loss, named so it stays named.** Reading the flush's range from `max(project_version)` off the operation log and letting the counter statement trail behind as a mirror passes the RANGE case, the consecutive-versions case, the monotonic case and the transaction case. **It is the sequence trap in a new costume:** two concurrent flushes read the same maximum and hand two operations the same version, so a client resyncing from the first never sees the second. Surfaced by the implementing window rather than by a test, which is the point: **the suite cannot distinguish it, so only prose can**, and it now lives in ADR-0004 decision 2's sharpening of 2026-08-10. A window under pressure to turn a red clause green is exactly who finds it.
## [2026-08-10] finding | **Django's autodetector splits a foreign key out of `CreateModel` into a trailing `AddField`, which breaks a composite primary key naming that field.** Reported by MAP-11's implementing window: `sqlmigrate` died with `FieldDoesNotExist: ProjectVersionCounter has no field named 'tenant'`, and inlining the reference back into `CreateModel` is what makes the migration loadable. **Recorded second-hand rather than re-derived**, which is stated because this log's own rule is that a claim carries the command that verified it; what was verified here is that the edited migration applies and `makemigrations --check` reports no changes. It is written down because the next composite key in this schema meets it, and because `.claude/rules/python-django.md` requires a migration to be generated and then edited, so the edit is expected while this particular reason for it is not obvious.
## [2026-08-10] finding | **Nothing on the flush path checks that a batch's project belongs to the tenant its principal was verified against, and reading two columns side by side is what made it visible.** Found at MAP-11's Window A correction review and filed as **MAP-39**; recorded in ADR-0004 decision 4's second addition, which was extended in the same pass because it had settled the counter's missing reference without noticing that the same evidence binds the log's new `project_id` column. `mapsift/sync/api.py` verifies the tenant claim through the membership and stops, which is ADR-0010 decision 6 working exactly as decided; **T6.4 puts access one level down, at the project, and nobody looks there.** The effect today is that an authenticated member of a tenant can address any project identifier at all and cause a log entry, and after MAP-11 a counter row, to be written under it, in the one table ADR-0004 calls extremely hot. **C4 is intact**, because the wall keys on the tenant and every such row lands inside the writer's own; the hole is in authorisation rather than in isolation, and conflating the two is how a wall gets blamed for a lock it never had. **What generalizes: a decision that removes a constraint should be read against everything that constraint was silently doing.** The foreign key was never there to prove a project was real, but writing down that it will not be there is the moment to ask who proves it instead, and that question does not surface while the two columns are decided one at a time.
## [2026-08-10] finding | **A new tenant-owned table can leave the isolation wall in silence, because the suite that proves the wall enumerates by the very column such a table would be missing.** Found at MAP-11's Window A review. `apps/api/conftest.py`'s `tenant_owned_tables` fixture reads the catalogue **filtering on the tenant column**, and every case in `tests/test_tenant_isolation.py` consumes that set, so a table built without it does not fail those cases: it **disappears from them**, and the suite stays green while C4 is open on that table. A width assertion does not close it either, since a two-column counter passes a narrowness check and stays outside the wall. **This is the general shape the `orchestrate` skill already names as this system's heaviest trap, met from a new direction: the wall denies by returning nothing, and here the wall's own test denies by enumerating nothing.** The guard is one assertion, that the new table appears in the enumerated set, and it belongs to the window that writes tests rather than to the one that writes code, which is where the review sent it back.
## [2026-08-10] decision | **A branch that goes `CONFLICTING` under a moved `main` is rebased locally and pushed with `--force-with-lease`, and the blanket "never force-push anything" that had no answer for it is replaced** (`dev-workflow` section 5, `pr` section 2 now pointing there instead of carrying a second copy). **The ordinary case was already answered and the first draft of this rule missed it**, which is the finding worth more than the rule: `reference.md` under "Repository protection" has prescribed **Update with rebase** (`gh pr update-branch --rebase`) for a `BEHIND` branch since 2026-08-04, together with the reason the plain update is wrong, so writing a general force-push rule would have created a second answer competing with a correct one. **Only `CONFLICTING` is genuinely unanswered**, because no server-side update resolves a conflict. **Measured rather than assumed** (`gh api repos/johnsilverio/mapsift/rulesets/<id>`): the ruleset's conditions are `include: ["~DEFAULT_BRANCH"]` with an empty exclude, so `non_fast_forward` covers `main` and no feature branch, which is what makes the lease push legal as well as safe. The lease is what makes it safe: unlike `--force` it refuses when the remote moved since the last fetch. **The general shape, and this canon has met it before from the other side: before writing a rule for a gap, grep for the rule that already covers the neighbouring case**, because a blanket prohibition with one exception carved into it is how two documents start answering the same question differently.
## [2026-08-11] decision | **The per-client cursor is keyed by clientID, tenant and project together, its absence is the row's absence, and the first mutation number is zero** (PRD M4 Shape and M10 Shape, both sharpened at the MAP-12 pickup). **CORRECTED the same day at that task's review, and the line is rewritten rather than left standing because a grep of this file has to return the rule that holds:** the key first written here was tenant and clientID alone, and the entry below carries what was wrong with it. The wall decides the tenant half: C4 carries a tenant identifier on every row, ADR-0005 section 8's exception is `FOR SELECT` only and a cursor is written, so a cursor without a tenant cannot be read by the tenant-bound role at all. The envelope decides the third: `MutationNumber` is generated as an unsigned integer with no floor above zero, so saying the first value is zero costs no change in `libs/core` and does not disturb the freshness gate, while choosing one instead would have dragged Rust and the generated contract into a Python-only slice. **What generalizes: when a requirement says "the first" of a client-owned axis and the generated contract already admits a value, the contract has already voted**, and picking against it is a change to two languages rather than a preference.
## [2026-08-11] decision | **LATE placed the dedup while the dedup was still only a read, so the cursor's write needed its own placement** (ADR-0004 decision 2, extended at the MAP-12 pickup). The bullet lists dedup among what runs before the allocation and that is correct for the comparison, which takes no lock; **the cursor is also a row somebody writes**, and when LATE was decided no such row existed. The halves separate: the cursor is read early as a validation, and written late in one statement beside the allocation with a guard refusing to move it backwards. **The shape refused, named because it is the obvious one:** `SELECT ... FOR UPDATE` on the cursor early, to make the read-then-write atomic, holds that row across the whole flush and reintroduces on a second table the exact cost RANGE and LATE were measured to remove. **The atomicity it reaches for is already bought:** the log's `UNIQUE (tenant_id, operation_id)` fails a duplicate insert whole, so the cursor is the cheap path and the constraint is the backstop. **What generalizes, and it is the third instance in this ADR: a rule written about a set of operations has to be re-read every time a member of that set changes what it costs.** Dedup moved from a read to a read plus a write, and the sentence naming it did not notice.
## [2026-08-11] decision | **A resent operation the server already holds is answered as applied rather than refused** (PRD T2.3 acceptance, added at the MAP-12 pickup). It reaches that state by arriving under a different mutation number and surviving the dedup filter. C12 exists to make the retry path safe, and a refusal there turns a legitimate resend into an error a client cannot tell apart from a real one.
## [2026-08-11] finding | **T2.3 recorded the cursor's expiry policy as wholly open while M4 had already settled its floor in the same PRD round, and M4's own Provenance says so.** Corrected in place at the MAP-12 pickup: T2.3's Open / ADR line now points at M4 and states nothing of its own. **What generalizes: a requirement that closes half of a sibling's open item has to strike the sibling's line in the same pass**, because the half that remains open keeps the line looking current, and a stale line that is still partly true is the kind nobody re-reads. Found by grepping the canon for the cursor before editing it, which is the fan-out step that exists for exactly this.
## [2026-08-11] finding | **A decision that partitions a request partitions every per-client guarantee measured across requests, and the two were decided one day apart by the same people without being read together.** Found by the Canon axis at MAP-12's Window A review; corrected in PRD M10's Requirement, PRD M4's Shape, PRD N10's acceptance, `.claude/rules/rust-core.md` and ADR-0010 decision 6. **ADR-0010 decision 6's addition of 2026-08-10 made a flush address exactly one project.** The MAP-12 pickup, on 2026-08-11, keyed the per-client cursor on tenant and clientID alone and wrote into M4 that M10's contiguity was measured the same way, **while M10's Requirement one line above the sentence being edited still read "contiguous per clientID"**. The concrete failure the axis traced: one installation with memberships in two tenants mints 0, 1, 2 from the single queue T1.2 describes, sends 0 and 2 to tenant A and 1 to tenant B, and A's flush shows a gap the client can never fill, so MAP-13's resend-from-cursor stalls that installation permanently, which is a C2 convergence failure reached entirely through documents. **The rule that resolves it is the gap rule's own justification read carefully:** a hole means loss only while the stream has one destination, and with several the server cannot tell "went elsewhere" from "was lost", so the contiguity domain must equal the flush domain. **The alternative was refused with a measurement rather than a preference:** widening the flush to a whole tenant lets one batch span projects, and such a batch cannot allocate its range in one statement, which is ADR-0004's RANGE rule and the fifty-six-fold number behind it. **Two general shapes, and the second is the one this canon keeps paying for.** A guarantee stated as "per X" is a claim about a domain, and it goes stale the moment anything else partitions that domain. And **editing one line of a requirement does not mean the line above it was read**: the document was open, the contradiction was one line away, and it still shipped.
## [2026-08-11] finding | **A task spec that shortens the clause it cites produces a test that pins the short form, and the spec that did it carried the warning against it in its own closing paragraph.** Found by the Spec axis at the same review. PRD T2.3's addition reads "answered as applied rather than refused, **so the flush succeeds and echoes**"; `specs/tasks/MAP-12-dedup-and-echoed-cursor.md` dropped the last five words, and Window A's case for that criterion then asserted only the status code. **Why the missing half was the load-bearing one:** that path is the only place where *applied* and *inserted* come apart, so an implementation echoing the pre-existing cursor there passes every case written, and under C12 the client advances only from the echo and resends that operation forever. **What generalizes, and it is narrower and more useful than "cite, do not restate": when a spec paraphrases a criterion at all, the paraphrase is a second copy inside the one document whose entire purpose is to avoid one.** The bullet is pasted verbatim or it is a pointer to the line; there is no safe middle.
## [2026-08-11] finding | **An axis reported a generated type as missing a constraint the generator had in fact emitted, and the orchestrator's verification is what kept it out of the canon.** The Craft axis reported that `MutationNumber` reaches `apps/api/mapsift/sync/envelope.py` as a bare `RootModel[int]`, so a negative mutation number would bind at the boundary and be silently dropped by dedup. **It is false**: `ge=0` sits four lines below the class statement the axis quoted. Recorded because the finding was plausible, specific, well-argued and would have opened a fidelity issue against ADR-0009 that nothing warranted. **What generalizes: a subagent's finding is evidence and not a verdict, and the cheapest ones to check are the ones that quote a file**, so check those first and check them by opening the file rather than by judging the argument.
## [2026-08-11] decision | **A flush addresses exactly one clientID, a fourth typed refusal, and the order the four batch agreements are checked in is part of the decision** (ADR-0010 decision 6, addition of 2026-08-11 at the MAP-12 Window A review). **The evidence that made it enforcement rather than a new rule:** `client_id` had **no reader in `apps/api` at all** until MAP-12, so its batch composition had never had to be decided, while every multi-operation batch in the sync suite already pinned one by hand under the variable name `one_client` and `test_project_version_allocation.py`'s helper stated the rule in its docstring, *one client's queue, in one project of one tenant*. Three windows wrote that independently; only the boundary was missing. **What forces it is the response contract and not a lock:** T2.3 echoes **the** last-applied number and C12 has the client advance from that echo alone, so a batch of two installations has no defined echo and the loser advances onto a number the server never applied for it. **The check order, tenant then project then clientID, is empirical:** four cases in `apps/api/tests/test_authenticated_request.py` build batches disagreeing on tenant or project **and** on clientID, because the factory mints a fresh one when a caller does not pin it, so a clientID check running first hands them the wrong refusal and the case at line 332, which asserts the status without the body, **keeps passing for the wrong reason**. **Two general shapes.** A field that travels on a contract but has no reader has no enforced rules either, and the day it gains its first reader is the day its composition has to be decided, not the day somebody notices. And **a convention practised identically by three independent windows is evidence of a decision nobody wrote down**, so the cheapest place to look for a missing rule is the shape the tests already take without being told.
## [2026-08-11] finding | **PRD M8's Open / ADR owes an ADR for "the wire encoding of the envelope and the batching format of a flush", MAP-33 carries only the wire-encoding half and cites only that, and the batching-format half has no issue and no home.** Noticed while placing the fourth batch composition rule, which had nowhere better to go than ADR-0010 decision 6, where the other three already live. Recorded rather than filed. **What generalizes: an Open / ADR line naming two things gets an issue for one of them, and the half that keeps its name in the requirement looks covered because the other half's issue quotes the same line.**
## [2026-08-11] version | **foundation v0.18: the per-client cursor is keyed by clientID, tenant and project, so both its cardinality and its stated cost move** (section 4, the v0.8 Decision block, sharpened). Logged as a round rather than a patch because the cost sentence was **false** rather than incomplete: it read that the server keeps one cursor per clientID growing with the number of instances, and it grows with instances **times** the projects each touches, which is the figure MAP-42's expiry and collection policy has to be sized against. I9 keeps its substance; what moved is a granularity the Decision block stated as a consequence. Fan-out reached: the current-version pointers in `CLAUDE.md` (project status, constraints header, authority chain) and `session-handoff.md`; PRD M4's Requirement and Shape, M10, N10, T2.3; `.claude/rules/rust-core.md`; ADR-0004 decision 2 and ADR-0010 decision 6; `specs/index.md` untouched because it names no version. **Three currency pointers were deliberately left and are named rather than moved:** the PRD's authority line and the `Authority:` lines of ADR-0009 and ADR-0010, because each claims the document has been **read** against the version it names and moving one without the reading converts an obvious gap into a false assurance. **The PRD's reads v0.17 and has been stale since v0.17.1 shipped on 2026-08-05**, surfaced by this round rather than caused by it. **Corrected at MAP-12's fourth review, where two axes independently found that the foundation's own entry for this round listed the PRD authority line among the targets reached while this line did not list it at all**, so the two records of one round disagreed; the entry that claimed it is the one that was wrong.
## [2026-08-11] finding | **The fan-out grep searched for the wording the correction had just introduced and never for the wording that already stated the old rule differently, so the same decision was corrected in five documents and left standing in three.** Found by the Canon axis at MAP-12's third review, reading the authority chain **upward** rather than across. The correction ran through PRD M4's Shape, PRD M10, PRD N10, ADR-0004 and ADR-0010; it did not reach **PRD M4's own Requirement one line above the Shape**, where the same rule lives as a lookup predicate MAP-13 was scheduled to implement, nor **the foundation**, which is the top of the chain and the tiebreaker `CLAUDE.md` points a disagreeing window at. The concrete failure the stale predicate admitted: an installation holding a cursor at 5 in one project opens a second, flushes from zero, and a literal implementation finds a cursor "for this clientID", deduplicates the whole queue away and echoes 5, which under C12 makes the client advance past work the server never took. **The general shape, and the governance rule already prescribes the remedy that was skipped:** grep for the **concept** and not only for the phrase you just wrote, because a document that paraphrased the decision does not cite its identifier and does not repeat your wording. **Three instances in one session, each one line or one document away from the last**, which is the argument for the axis reading upward rather than for trying harder.
## [2026-08-11] decision | **The flush response is `{"last_applied_mutation_number": <integer>}`, and ADR-0010 decision 6's declination to constrain the response body is retired rather than left to lapse** (addition of 2026-08-11). Found by the Spec axis as a constant in a test module, which is a public wire contract decided in the one place with no reader outside the suite. **The key names its axis** because M10 forbids any code path reading one version axis as another and this body grows: MAP-22 adds the per-project version to the same object, and a bare `last_applied` would sit beside a second number it could be confused with. **What generalizes: a declination is only good while the thing declined does not exist**, so the task that creates it inherits the obligation to convert the declination into a decision, and nothing marks that moment except somebody noticing.
## [2026-08-11] finding | **An orchestrator handed a window a wrong inference as measured evidence, and the protocol's own rule about evidence is what stopped the window questioning it.** Found by the Spec axis at MAP-12's Window B review. The task spec's evidence block carried "a grant assertion written against the owner proves nothing", drawn from ADR-0005 section 2's correction of 2026-08-07 that the append-only grant does not reach the owner profile. **The premise is true and the inference is false, and the two are one sentence apart:** a connection as the owner proves nothing about **its own** privileges, and it is exactly what can ask about **another role's**, because `has_table_privilege(<role>, <table>, <privilege>)` takes the role as a parameter. `test_append_only_log.py` has asserted the log's whole grant that way since MAP-10, in the same package, and nobody looked. **What it cost:** the wrong claim reached a module docstring on disk, and two narrow tables (`sync_projectversioncounter` from MAP-11, `sync_clientcursor` from MAP-12) shipped with correct grants and no guard, so an `UPDATE` dropped from either breaks the flush in production with the whole suite green. **What generalizes, and it is the sharpest process lesson of this task: `specs/testing.md` section 1.1 tells a window that handed-over evidence is not a decision it should be making, so an error in the evidence block rides through unchallenged by construction.** That is the same property the briefing rule warns about for instructions, applied to facts, and it means an evidence bullet is held to a higher standard than a rule in the same prompt, not a lower one. **The cheapest check is the one that was available all along:** an evidence claim about what a suite can or cannot do is checked by grepping the suite, and this one was refuted by a test forty lines from the module that repeated it.
## [2026-08-11] finding | **A real measurement was attached to the wrong span, and the conclusion it supported survived while its magnitude was off by an order of magnitude.** Found by the implementing window at MAP-12's second Window B round, against ADR-0004 decision 2's extension written hours earlier. The paragraph claimed that writing the per-client cursor after the append moves the **11.99 ms entry-building span** onto the shared version counter. It does not: **this decision's own sharpening of 2026-08-10 already put the entry materialisation before the allocation**, so that span sits outside the counter's held window in both orders, and the true cost of the swap is **one statement** on the shared row. Cursor-first still wins and the code was already right, so nothing changed but the sentence. **What generalizes, and this canon has now carried it in three costumes:** the orchestrator failure mode is not inventing a number, it is **carrying a real one past the configuration it was measured in**. Here the number was correct, its source was correct, and only the span it was attached to was wrong, which is the hardest form to catch because every checkable part of the sentence checks out. **What caught it was an implementer forced to write a comment about the mechanism**: its first draft named the mechanism in the ADR's own terms, the mechanism did not match the code, and the window then wrote a comment that asserts no magnitude at all so it cannot drift into being wrong. **A pointer that cites a decision outlives a paraphrase that explains it**, which is the comment rule arriving at the same place from the other direction.
## [2026-08-13] decision | **The flush gains a second answer: a gap against the per-client cursor is `409` with a closed object carrying a reason from a set of two, `gap_above_cursor` with an integer restart point and `no_cursor_in_this_domain` with none.** Closed at the MAP-13 pickup and recorded in **ADR-0010 decision 6**, addition of 2026-08-13. `200` was refused because a client reading the status and not the body would take a refusal for an acknowledgement and then let its own dedup drop the operations the gap reported, which is the silent loss the response exists to prevent reintroduced one layer up; `422` was refused because the batch satisfies the contract that decision declares and the gap is undecidable from the body alone. **The two reasons stay distinct against MAP-13's own issue, which treats them as one:** an absent cursor is a gap from nothing for the comparison and not for the remedy, since a client whose cursor was collected may no longer hold what a resend would ask for.
## [2026-08-13] decision | **A batch whose own mutation numbers skip or arrive out of ascending order is a fifth composition rule, refused at the Pydantic boundary with `422` beside the other four.** Closed at the MAP-13 pickup and recorded in **ADR-0010 decision 6**, addition of 2026-08-13. The split is by decidability: what the request body proves alone is refused where nothing has been bound, and only the comparison against the stored cursor is taken after the binding. **Ascending order is part of the rule rather than a nicety**, because `append_to_the_operation_log` stamps the per-project version in list order, so a batch contiguous as a set and shuffled as a list records a chain in an order the client never authored and PRD M15 replays that order as evidence.
## [2026-08-13] decision | **PRD M10's gap clause is narrowed from "applies nothing from the gap onward" to "applies nothing at all".** Closed at the MAP-13 pickup as an in-place dated sharpening of M10's acceptance, no version bump. The wider form permits a flush that both applies and refuses, and its answer would then have to carry a moved cursor and a restart point at once, while C12 has the client advance from one echo and nothing else. Nothing in ADR-0004 changes: a refused batch never reaches the allocation, so decision 2's guard that the allocation is not reached with an empty batch already covers it by construction.
## [2026-08-13] finding | **Two state claims were false at the 2026-08-13 boot, and both were counts, which is the one kind of claim that rots with nobody editing it.** `session-handoff.md` section 0 said the tracker held forty-four issues where Linear holds forty-three, and `.claude/skills/README.md` said the hooks suite had thirty cases where it has thirty-three. **The corollary the earlier instances did not carry:** the recurring failure this project logs is a measurement generalized past its configuration, which needs somebody to write a wrong sentence, while a count is wrong the moment the set it counts changes somewhere else, so it decays passively and reads as verified. A count in prose is therefore a claim with a shorter shelf life than the paragraph around it, and the fix that survives is to write what the set is for rather than how many it has.
## [2026-08-13] decision | **The gap response's restart point is the cursor plus one, the first mutation number the server has not applied.** Closed at MAP-13's Window A review and recorded in **ADR-0010 decision 6**, addition of 2026-08-13. **It is the addition of 2026-08-11's own defect arriving one round later**, a public wire contract living as a constant in a test module, and PRD M10's prose pulled the other way by asking the client to "resend from the cursor". Plus one wins because the dedup already keeps operations strictly above the cursor, so anything else gives one axis two meanings, and because a key spelled `resend_from` handed the cursor itself forces the client into an off-by-one. **Found by three readers who did not share a context:** the implementing window reported it as the one choice it was not confident in, and the Canon and Spec axes reached it independently.
## [2026-08-13] decision | **The fifth composition rule is restated as mutation numbers that do not ascend by exactly one at every step, because its first form did not refuse a repeat.** Corrected in **ADR-0010 decision 6** hours after being written, at MAP-13's Window A review. The first form read "skip, or arrive out of ascending order", and **a batch repeating a number is neither**: a repeat is non-decreasing and skips nothing. Two things rested on the hole. PRD M10's Shape calls the axis contiguous and a repeat is not, so a real violation had no refusal at all; and the same addition's positional argument is about batches that break the rule **by repeating zero**, so it argued from a refusal that would never have fired. **The general shape, and it is the sharper half:** an enumeration of the ways a rule can be broken is a claim that the list is complete, and it fails silently where a case is neither of the two things the author happened to picture.
## [2026-08-13] finding | **A narrowed acceptance clause lost the case that would witness the narrowing, and the loss was invisible in the same document that made it.** PRD M10's gap clause was narrowed to "applies nothing at all" hours before the composition rule made the contrast unfalsifiable: a batch reaching the cursor comparison is already internally contiguous, so it carries no appliable prefix and the narrow and wide forms coincide. Recorded on M10 rather than reverted, because the clause still refuses an implementation that answers the refusal and appends anyway. **The corollary:** two rules written the same day into two documents can each be right and jointly remove a test, and neither author sees it, because each is reading its own document. Reached independently by the implementing window and by the Spec axis; neither the orchestrator who wrote both nor the Canon axis saw it.
## [2026-08-13] finding | **A decision sharpened later in the same session needs its own fan-out, and the earlier sweep is exactly what stops it happening.** Found by the Canon and Spec axes at MAP-13's Window A review, one blocking finding each and both in documents the orchestrator owned. The response shape was fanned out at the pickup, and the reverse grep **did** hit PRD M10's Requirement, which was left alone correctly, because "resend from the cursor" contradicted nothing while only the shape was decided. Hours later the **value** was ratified as the cursor plus one, and that second verdict reached the ADR, `log.md` and `canon-checks.md` while the sentence the ADR itself names as reading the other way stayed untouched, together with two lines of the task spec quoting the retired phrasing of the fifth composition rule. **The mechanism is the completed first sweep:** it leaves a memory of having swept, and a refinement of the same decision is read as covered by it. **The corollary, and it is the operational half:** the unit that needs a fan-out is a **verdict**, never a topic, so a second verdict on a topic already swept starts its own sweep from zero. Left in the higher authority, the defect would have shipped an off-by-one on two sides of one boundary, since MAP-15 and MAP-19 trace to M10 and not to the ADR.
## [2026-08-13] finding | **A test rewritten to describe the protocol faithfully caught an implementation the unfaithful version accepted, so fidelity in a protocol suite is coverage rather than presentation.** Found at MAP-13's Window A third round. The resend case originally minted **fresh** operation identifiers for the operations a refused flush was asked to send again, which no real client can do, since its queue is persistent and append-only and the identifier is client-minted and stable (I3). Binding the resend to the identifiers the refusal was about was taken as a fidelity correction, and it brings under test a server deduplicating by `operation_id` instead of by the per-client cursor, which C12 forbids and which the fresh-identifier form left unwitnessed. **That last clause is a property of the implementations this task admits and not a run**, and the boundary is drawn here because the window drew it rather than letting the claim pass: with the refusal unimplemented every case in the module dies at its own status assertion, so nothing observable today separates the two identifier choices. **The general shape:** a suite that stands in for a protocol is read by whoever builds the other half of it, here MAP-15 and MAP-19, so a fixture that does something no client can do both teaches the wrong contract and hides every implementation that only works because the fixture was unrealistic. **The corollary that made it worth a comment:** the correct form now looks like a copy-paste artifact, and minting fresh identifiers reverts it while breaking nothing visible, which is the trap `CLAUDE.md` reserves an inline comment for.
## [2026-08-14] finding | **A requirement recorded that no test could witness a distinction, and a test written in the same round declared itself that witness.** PRD M10's Shape says the narrowed gap clause "must not be read as pinned by a case that tells the two forms apart, because none can exist while the composition rule stands"; the docstring of `test_a_flush_starting_above_the_cursor_applies_nothing_at_all`, written the same day under the same task, claims exactly that discriminating power. **Measured at the MAP-45 probe, 2026-08-14:** against an implementation that answers the refusal correctly and appends the batch anyway, the case stays **green**, along with its four state-reading siblings, and the only case that goes red is `test_a_refused_flush_takes_no_per_project_version`. **The mechanism is one level simpler than the reading the orchestrator handed over**, which named the tenant-and-user transaction nesting: the append sits inside an `atomic()` block the refusal exits by exception with the catch outside it, so the write is discarded whatever the nesting does. Isolated by moving only the catch inside the binding, which turns one failure into four, while the same move against the correct implementation changes nothing at all. **What generalizes: a fan-out reaching two documents is not the same as the two being read against each other**, and the pair that most needs it is a requirement stating a limit beside a test claiming to exceed it, because each is correct alone and only the conjunction is false. **The corollary the probe earned: a guarantee witnessed through a side effect is witnessed by accident**, since that surviving case catches the append only through the version allocation the append happened to need, and an implementation sourcing the version elsewhere walks past it.
## [2026-08-14] finding | **`disable-model-invocation: true` stops a skill from firing and never from being followed, and the orchestrator read it as a wall twice in one session.** The owner asked for `/github-workflow` and later for `/session-handoff`, and both times the answer was that the model cannot fire them, so the request went back to the owner. **The flag governs automatic invocation, not the contract**, and the contract is a file one `Read` away; the caution already written in this handoff even says the orchestrator "then follows the skill's contract by hand", where by hand means opening it. **A second mechanism was mistaken for the first and it is worth telling them apart:** a slash command typed **inside a sentence** does not register as a command at all, which is what actually happened both times, and removing the flag would not have changed that, because the flag decides whether the model may invoke and the parser decides whether the owner's typing becomes a command. **What generalizes: a guard that stops you doing something on your own initiative is not a guard against doing it when asked**, and treating it as one converts a safety property into friction while leaving the safety unused. The corollary that costs the most: **refusing on a config you have not opened is the same failure as asserting from memory**, since both substitute an inference for a file that was there to be read.
